---
import { ArrowUpRightIcon } from "@phosphor-icons/react";
import { primaryLinks, site } from "../data/site";
import HeroVisualStage from "./HeroVisualStage";
import SectionGuide from "./SectionGuide";
import TypewriterLine from "./TypewriterLine";
---

<section id="top" class="hero-section" data-reveal="hero" data-motion-group>
  <div class="shell hero-grid">
    <div class="hero-copy" data-motion-item data-motion-kind="hero-copy">
      <p class="direction-line"><TypewriterLine client:load /></p>
      <h1>{site.title}</h1>
      <p class="hero-intro">{site.introduction}</p>

      <div class="focus-line">
        <span class="pixel-type">CURRENT FOCUS</span>
        <div>
          <strong>C++ 锚点</strong>
          <strong>Rust 成长</strong>
          <strong>图形工具</strong>
        </div>
      </div>

      <div class="hero-actions">
        <a class="command command-primary" href="#projects">
          <span>看项目</span>
          <ArrowUpRightIcon size={18} weight="bold" />
        </a>
        <a class="command command-secondary" href="#contact">联系我</a>
      </div>

      <div class="quick-links">
        {primaryLinks.slice(0, 3).map((link) =>
          link.href ? (
            <a href={link.href} target={link.external ? "_blank" : undefined} rel={link.external ? "noreferrer" : undefined}>
              {link.label}
            </a>
          ) : (
            <span aria-disabled="true">[{link.label}]</span>
          ),
        )}
      </div>
    </div>

    <div class="hero-stage-entrance" data-hero-stage-entrance data-motion-item data-motion-kind="hero-stage">
      <HeroVisualStage client:load />
    </div>
  </div>
  <SectionGuide target="#about" label="前往关于我" />
</section>

# 开发日志

## 2026-08-16 / v0.3 Motion Foundation

今天完成：

- 新增全站章节进入与离开协议，使用 IntersectionObserver 反复触发，不监听滚动帧。
- 为 Hero、关于、技术带、项目、文章、生活与联系建立统一的可见状态。
- 加入 `420ms` 离开防抖，避免阈值附近滚动造成闪烁。
- 章节状态通过 `homepage:section-presence` 事件开放给后续 WebGPU 世界。
- reduced motion 或不支持 IntersectionObserver 时直接展示内容，不保留位移和透明度动画。

当前现象：

- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：成功。
- 本阶段没有修改项目数据、卡牌状态机或背景渲染器。

下一步最小动作：

- 创建项目侧板与卡牌编排分支，将问题、过程、证据同步到桌面侧板和移动摘要。

阻塞与证据：

- 无阻塞。

## 2026-08-16 / v0.3 Penpot V7

今天完成：

- 保留 Penpot V6 原稿，新建 V7 桌面主页、移动主页、交互基础、卡牌分镜和 WebGPU 动态世界说明五张画板。
- 将桌面项目空白区改为与当前卡同步的问题、过程、证据侧板，并增加紧凑牌组轨道。
- 为项目卡补充 GitHub、Demo 和更多证据的链接状态；移动端使用卡后证据摘要，避免把完整侧板压进窄屏。
- 以 `0 / 180 / 580 / 820ms` 四个状态记录旧牌回背、来牌翻转、粒子冲量和末端回弹。
- 记录 WebGPU → WebGL2 → 静态 CSS 降级路径、数据鳐行为、背景后处理限额和可重复章节动画。
- 导出五张 PNG 并检查文字裁切、元素重叠、对比度和移动端溢出；第二轮清理旧列表透出与翻面文案重叠。

当前现象：

- V7 设计已完成，等待确认；本阶段没有修改生产代码。
- 桌面与移动稿延续博客偏蓝配色，珊瑚红仅用于冲量、证据和注意力标记。
- Penpot HTTP MCP 可用，插件面板需保持打开；长画板导出耗时约 30–80 秒。

下一步最小动作：

- 用户确认 V7 后，从 `develop` 创建 `feature/motion-foundation`，先实现统一 motion 协议和 reduced-motion 基线。

阻塞与证据：

- 无技术阻塞；生产实现受设计确认门禁约束。

新增 Backlog：

- 用真实 SceneScope 仓库和演示 URL 替换设计中的链接占位。
- 生产实现时用 WebGPU capability 标记验证实际 backend，不只检测 API 是否存在。

## 2026-08-16 / Repository

今天完成：

- 确认 Penpot V6 为实现基线。
- 完整阅读职业发展文件，提取内容边界。
- 只读审计现有博客的蓝色主题、面板层次与主题切换方式。
- 初始化独立 Git 仓库并定义分支流程。

当前现象：

- `D:\project\homepage` 是新仓库，尚未建立 Astro 工程。
- 姓名、人像、简历、GitHub 和邮箱均未提供。
- 文章题目来自职业规划中的候选方向，尚未确认真实发布状态。

下一步最小动作：

- 从 `develop` 创建 `feature/foundation`，建立可运行的 Astro + Bun 骨架和主题变量。

阻塞与证据：

- 无阻塞。

新增 Backlog：

- 用真实资料替换个人占位符。
- 用真实 SceneScope 截图替换项目截图槽位。
- 接入已发布文章的真实链接和发布日期。

## 2026-08-16 / Foundation

今天完成：

- 建立 Astro 5 + Bun 工程与 React island 支持。
- 建立浅色、深色语义变量和系统主题首屏初始化。
- 自托管中文、拉丁与局部像素字体。
- 建立项目、文章和站点信息的数据模型。
- 完成语义页面骨架、SEO 基线和明确个人资料占位符。

当前现象：

- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：静态首页构建成功。
- 依赖安装首次遇到注册表连接拒绝，切换镜像后完成并生成 `bun.lock`。

下一步最小动作：

- 将静态骨架重构为 V6 叙事布局，完成响应式导航、关于我、文章、生活和联系区域。

阻塞与证据：

- 无阻塞。

新增 Backlog：

- 增加可访问的主题切换控件。
- 将作品列表改造成可扩展卡组，不把数量写死在视图层。

## 2026-08-16 / Narrative

今天完成：

- 按 Penpot V6 重构首屏、关于我、作品、文章、生活与联系区域。
- 建立人物素材、项目截图、文章缩略图和生活照片的明确占位区域。
- 增加浅色与深色主题切换、移动端全屏导航和 Escape 关闭行为。
- 使用像素字体、像素角标、空间轴与叠层表达技术气质，不使用受版权保护的角色素材。
- 作品数据仍由数组驱动，页面没有写死三张卡牌的上限。

当前现象：

- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：静态首页与 React 导航 island 构建成功。
- 当前作品卡是静态首张状态，按钮行为将在交互阶段接入。

下一步最小动作：

- 在独立交互分支实现卡牌切换、翻面、触摸滑动和键盘操作。

阻塞与证据：

- 无阻塞。

新增 Backlog：

- 为首屏加入自适应 Three.js 世界背景。
- 为类型状态加入克制的打字机效果。
- 为卡牌切换加入像素粒子反馈和 reduced-motion 降级。

## 2026-08-16 / Interaction

今天完成：

- 用 Three.js 建立铺满首屏的实时世界背景，并按设备能力调整像素粒子数量和 DPR。
- 在页面不可见、标签页隐藏或系统要求减少动态效果时暂停或降级动画。
- 为职业方向加入打字机状态行，保留静态文本作为无脚本和 reduced-motion 回退。
- 将代表项目改造成数据驱动的可扩展卡组，项目数量不受视图层限制。
- 完成分类筛选、循环切牌、卡牌翻面、指针倾斜、触摸滑动和键盘操作。
- 卡牌背面统一呈现问题、过程与证据，切换时加入克制的像素粒子反馈。

当前现象：

- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：静态构建成功，Three.js、Motion 与 React islands 正常产出。
- WebGL 不可用时保留静态背景；交互控件具备可见焦点和语义标签。

下一步最小动作：

- 从 `develop` 创建独立测试分支，记录桌面、移动端、主题、交互和画布验收结果。

阻塞与证据：

- 无阻塞。

新增 Backlog：

- 用真实项目截图和可公开的商业开发证据替换占位内容。
- 根据正式内容长度复核卡牌背面排版与筛选分类。
- 后续评估是否用 wgpu/Wasm 替换首屏渲染实验，但不纳入首版范围。

## 2026-08-16 / Browser QA

今天完成：

- 在 1440 x 900 桌面视口和 390 x 844 移动视口完成深浅主题视觉验收。
- 验证移动菜单、项目筛选、循环切牌、翻面、触摸滑动与键盘操作。
- 检查文字裁切、元素重叠、水平溢出、颜色对比度和首屏章节提示。
- 验证 Three.js 画布非空、可见性暂停、reduced-motion 和静态背景降级。
- 将验收矩阵、数值证据和内容边界写入 `docs/QA_REPORT.md`。

当前现象：

- 浏览器控制台无 warning 或 error。
- 桌面与移动端均无水平溢出；关键交互均通过鼠标、触摸或键盘验证。
- 姓名、联系信息、项目图片与文章仍是明确占位内容，不影响原型验收。

下一步最小动作：

- 在 `develop` 重新运行完整检查，合入 `main` 并标记 `v0.1.0`。

阻塞与证据：

- 无阻塞。

新增 Backlog：

- 获得真实个人资料后建立独立内容分支替换占位符。
- 接入正式域名之前补充站点图标、Open Graph 图片和部署配置。

## 2026-08-16 / Card Deck Motion

今天完成：

- 修正正反面在指针倾斜状态下产生不同透视位置的问题，翻面前统一归零外层倾斜。
- 将正反面改为同一固定卡高和同一套五行网格，标题、内容区、技术栈与翻面按钮共享空间基准。
- 移除串行等待切牌，改为旧牌退场与新牌从后方上桌同时发生的 spring 旋转过渡。
- 用两张真实项目预览替换装饰性卡片阴影，只渲染当前牌和后两张，数据规模仍不受限制。
- 后牌支持点击与键盘切换，并保留方向键、翻面键和移动端拖动操作。
- 修复旋转后的后牌覆盖下方切牌按钮、导致一次点击跳过两张的问题。
- 翻面后将键盘焦点移动到当前可见面的控制按钮。

当前现象：

- 桌面端正反面边界均为 `520 x 650`，坐标完全一致。
- 移动端正反面边界均为 `321 x 600`，四张项目卡的正面和证据区均无内容溢出。
- 1440 x 900 与 390 x 844 视口均无页面水平溢出。
- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：生产构建成功。

下一步最小动作：

- 在独立测试分支记录卡组补丁验收结果，合入 `develop` 后发布 `v0.1.1`。

阻塞与证据：

- 无阻塞。

新增 Backlog：

- 接入真实项目图片后复核不同图片明暗对卡牌空间层级的影响。

## 2026-08-16 / Playable Project Cards

今天完成：

- 将切牌改造成明确的五段状态：盖回卡背、收回牌堆、抽取新牌、翻到正面、恢复可操作。
- 首次进入项目区时按远牌、近牌、主牌的顺序逐张发牌，主牌先展示装饰卡背再翻正。
- 卡背只保留像素网格、几何轨道和牌组核心纹样，不再承载项目信息。
- 卡面加入作品图槽、真实语义的属性词条、技术标签、项目动作和正面详情层。
- 项目链接升级为可扩展 `links[]`；资料没有提供真实 URL 时显示“公开链接待补充”，不生成虚假外链。
- 新增全页 Canvas 环境层，以固定种子生成像素和简单几何，并支持鼠标视差与近距离粒子响应。
- 背景在无鼠标输入时也会独立改变位置、大小、亮度和轮廓，变化使用随机目标与缓动，不产生逐帧噪声抖动。
- 为减少动态、省流量、移动端和低核心数设备保留相应降级路径，并在标签页隐藏时暂停动画。

当前现象：

- 桌面卡面为 `520 x 690`，移动端约为 `321 x 650`，四张项目卡和详情层均无内容溢出。
- 单次切牌约 `1.16s`，过渡期间锁定输入，因此一次点击或按键只抽取一张牌。
- 桌面悬停、键盘聚焦后的按钮操作和移动端点击均可打开详情。
- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：静态生产构建成功。

下一步最小动作：

- 从功能分支合入 `develop`，在独立测试分支记录完整验收后发布 `v0.2.0`。

阻塞与证据：

- SceneScope 仓库、Demo 和案例文章 URL 尚未提供，因此项目动作保持明确占位状态。

新增 Backlog：

- 获得真实链接后只修改 `src/data/projects.ts`，不改卡牌组件结构。
- 接入真实项目截图后复核详情浮层与图片焦点区域的关系。

## 2026-08-16 / v0.3 Interaction Design Planning

今天完成：

- 审计 v0.2.0 的卡牌定时器状态机、一次性发牌触发、双背景实现和项目区信息分配。
- 确认卡顿来自固定等待、组件重挂载、弹簧收敛和层级切换叠加，不把问题简化为单一缓动曲线。
- 将下一版统一为“活体技术世界”方向，并定义信息层、交互层和环境层的动态预算。
- 规划可重复章节入退场、固定槽位卡牌时序、项目牌组侧板、程序化数据鳐和背景专用后处理。
- 根据新增约束将背景确定为 WebGPU 优先、WebGL2 降级、静态 CSS 兜底，并确认当前 Three.js 版本具备对应 renderer、fallback backend 和节点后处理能力。
- 将唯一推荐方案、实施分支和验收标准写入 `docs/V0.3_INTERACTION_DESIGN.md`。

当前现象：

- 本阶段只修改文档，没有改动页面实现、依赖或博客项目。
- 完整方案明显超过一次小修范围，拆分为 Motion、项目侧板、WebGPU 动态世界、后处理和浏览器验收五个阶段。

下一步最小动作：

- 等待方案确认；确认后从更新后的 `develop` 创建 `feature/motion-foundation`，只实施第一阶段。

阻塞与证据：

- 无技术阻塞；实现开始前需要用户确认 v0.3 方案。

新增 Backlog：

- 后续获得真实项目链接与图像时，在独立内容分支替换占位内容。
- v0.3 验收时分别记录 WebGPU 主路径和 WebGL2 fallback 的运行证据。

## 2026-08-16 / v0.3 Motion Foundation

今天完成：

- 建立可重复触发的章节进入与离开协议，Hero、关于、技术带、项目、文章、生活和联系区均已接入。
- 使用 IntersectionObserver 与 420ms 离开迟滞，避免章节边界附近反复闪烁。
- 章节重新进入时保留主题、项目筛选和当前卡等交互状态。
- 向背景层派发 `homepage:section-presence` 事件，为统一动态世界提供章节状态桥接。
- `prefers-reduced-motion` 下取消位移与裁切，直接显示稳定内容。

当前现象：

- 章节从上方或下方重新进入均可再次播放，滚动过程不被劫持。
- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：静态生产构建成功。

下一步最小动作：

- 在独立分支完成项目牌组侧板与重叠切牌时序。

阻塞与证据：

- 无阻塞。

## 2026-08-16 / v0.3 Project Sideboard

今天完成：

- 将固定定时器切牌重构为 Motion animation sequence，以重叠时间线完成旧牌翻背、回堆、新牌抽取和翻正。
- 单次切牌总时长收敛到约 820ms，出牌和来牌作为同时存在的独立实体参与过渡。
- 项目区重新进入视口时重新发出当前可见牌组，但保留筛选和当前项目。
- 桌面左侧加入与当前卡同步的 Problem / Process / Evidence 证据侧板和可扩展牌轨。
- 移动端加入卡后证据摘要；真实 URL 仍是项目动作出现的唯一条件。
- 为每个过渡卡实例生成唯一详情标识，并让隐藏卡面退出键盘交互。
- 同项目筛选直接更新牌组范围，不播放“同一张牌翻到自己”的无意义动画。

当前现象：

- 1440 x 1000 桌面项目区的侧板、当前牌和后两张牌均保持完整层次。
- 390 x 844 移动视口无水平溢出，卡牌与证据摘要均处于页面安全宽度内。
- 浏览器复验中，切牌过渡没有重复 DOM `id`，隐藏卡面内可聚焦元素为 0。
- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：静态生产构建成功。

下一步最小动作：

- 合入 `develop`，从更新后的集成分支创建 `feature/webgpu-ambient-world`。

阻塞与证据：

- SceneScope 仓库、Demo、简历和联系方式尚未提供，相关动作继续保留明确占位。

## 2026-08-16 / v0.3 WebGPU Ambient World

今天完成：

- 删除 Hero WebGL 与全页 Canvas 2D 两套旧背景，合并为单一固定 `AmbientWorld` 场景。
- 使用 Three.js `WebGPURenderer` 作为主路径；无 WebGPU 时进入 WebGL2 backend，初始化失败或主动降级时保留静态 CSS 几何。
- 通过 `data-backend="webgpu|webgl2|static"`、`data-quality` 和 `data-status` 暴露可验收运行状态。
- 在同一场景中加入低面几何、像素粒子、指针吸引与排斥、点击脉冲、抽牌冲量和抽象数据鳐。
- 数据鳐使用游走、指针场、几何避障、内容安全区排斥与边界力，不引用受版权保护的角色形象。
- 背景监听 `homepage:section-presence`，在 Hero、关于、技术带、项目、文章、生活和联系区之间平滑切换能量参数。
- 增加只覆盖画布的轻量扫描、暗角和色调统一层，不对 DOM 文本、图片或按钮做后处理。
- 限制桌面与移动 DPR、粒子数量和后端质量；页面隐藏时暂停，恢复时重置帧间隔。
- 将项目章节 reveal 属性移到 Astro 外层，消除 `client:visible` React 岛的 hydration mismatch。
- 保留 `?ambient=webgl2` 与 `?ambient=static` 作为本地强制验收入口。

当前现象：

- 默认路径实测为 `webgpu / full / ready`；强制降级实测为 `webgl2 / balanced / ready` 和 `static / static / ready`。
- 1440 x 900 桌面与 390 x 844 移动视口无水平溢出，桌面和移动 Hero 均在首屏保留下一章节提示。
- 移动项目摘要可展开，卡片 `scrollWidth === clientWidth` 且 `scrollHeight === clientHeight`。
- WebGPU 局部截图在无指针输入的 1.2 秒内哈希变化，证明空闲几何、粒子和数据鳐持续更新。
- WebGPU、WebGL2、静态路径及深浅主题复验后，控制台 warning/error 均为空。
- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：静态生产构建成功，无构建告警。
- WebGPU 场景 chunk 为 687.81 kB minified / 191.12 kB gzip，这是当前 Three.js WebGPU renderer 的主要首屏成本。

下一步最小动作：

- 合入 `develop`，建立 `test/v0.3-browser-qa` 完成最终回归并更新 `docs/QA_REPORT.md`。

阻塞与证据：

- 无功能阻塞。真实照片、项目截图和对外链接仍需用户提供。

新增 Backlog：

- 获得真实媒体后复核背景安全区和照片对比度。
- 若实机低端设备数据证明 191.12 kB gzip 仍影响首屏，再评估延迟启动或 Rust + wgpu/Wasm 替换，不在无数据时提前扩展架构。
- 高级 TSL selective bloom 继续保持关闭；只有当前轻量后处理不足且性能预算允许时才单独加入。

## 2026-08-16 / v0.3 Browser QA

今天完成：

- 在独立 `test/v0.3-browser-qa` 分支完成桌面与移动端、浅色与深色的浏览器回归。
- 复验移动菜单的打开、Escape 关闭和锚点导航关闭行为。
- 复验项目牌组的键盘切牌、详情展开、唯一 DOM 标识和移动端内容边界。
- 让项目区离开视口后重新进入，确认发牌周期再次触发，同时保留当前卡与主题。
- 分别验证 WebGPU、强制 WebGL2 和静态降级路径，三条干净加载会话均无 warning/error。
- 检查可见文案、页面水平溢出以及移动卡面内部溢出。

当前现象：

- 桌面 1440 x 900 与移动 390 x 844 均无水平溢出，浅色和深色主题正常。
- 默认桌面为 `webgpu / full / ready`，默认移动为 `webgpu / balanced / ready`。
- 强制后端分别为 `webgl2 / balanced / ready` 与 `static / static / ready`。
- 项目重入后发牌周期从 `1` 递增到 `2`，商业 C++ 当前卡和深色主题保持不变。
- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：静态生产构建成功，无构建告警。

下一步最小动作：

- 将测试分支以 `--no-ff` 合并回 `develop`，保留 `main` 作为后续正式发布门禁。

阻塞与证据：

- 无实现阻塞。姓名、照片、简历、联系方式、项目媒体和真实外链仍待用户提供。

## 2026-08-18 / v0.4 Emergent Ambient

今天完成：

- 保留 DeepSeek 已加入的 MIT License 与 ICP 备案信息，并将对应 `main` 提交同步回 `develop` 后再创建本功能分支。
- 删除技术关键词横条，不再用重复技术名词打断个人叙事。
- 将项目左侧 Problem / Process / Evidence 证据侧板收敛为可扩展牌组索引，详情正文只保留在卡面的一处内容源。
- 将浅色主题从冷灰蓝调整为中性瓷白，同时为动态实体增加可辨识填充、描边和接触阴影。
- 将背景对象拆分为不参与碰撞的像素特效、可碰撞几何实体和程序化八足生物。
- 加入包围圆碰撞、速度交换、旋转与命中反馈，以及圆形、长方形、三角形的有界破碎规则。
- 生物行为由游走目标、附近实体、内容安全区、边界和指针斥力共同驱动，可以踢开实体并触发破碎。
- 鼠标对实体和生物只产生斥力；快速移动和点击脉冲提高弹开冲量，不把背景对象吸向指针。
- 将碰撞、鼠标斥力、破碎模式和实体上限抽到纯函数模块，并新增 5 个 Bun 单元测试。
- 修正 WebGL2 质量标记与真实资源预算不一致的问题；WebGL2 现在实际使用移动级粒子数、实体数、DPR 和抗锯齿配置。
- 背景 `data-status` 改为首帧仅写一次，避免在渲染循环中重复更新同一个 DOM 属性。

当前现象：

- 桌面默认路径实测为 `webgpu / full / ready`，实体数稳定在上限 14，破碎计数能在无输入时自然增长。
- 移动默认路径实测为 `webgpu / balanced / ready`，初始实体数为 6，页面无水平溢出。
- 强制路径实测为 `webgl2 / balanced / ready` 和 `static / static / ready`，两者均无控制台 warning/error。
- 浅色页面基底为 `#f7f8f6`，动态实体在浅色和深色中均可辨识。
- 项目切牌实测约 0.9 秒，旧牌翻回卡背与新牌由卡背翻正均在过渡中可见。
- 项目区离开后再次进入，发牌周期从 `1` 增加到 `2`；牌组状态和主题保持不变。
- ICP 备案链接实测为 `湘ICP备2026030115号-1`，目标为工信部备案系统。
- `bun run test`：5 passed、0 failed。
- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：静态生产构建成功。

下一步最小动作：

- 将功能分支以 `--no-ff` 合入 `develop`，再建立独立 `test/v0.4-browser-qa` 分支记录最终回归。

阻塞与证据：

- 无实现阻塞。姓名、人像、简历、联系方式、项目媒体与真实外链仍待用户提供。

新增 Backlog：

- 获得真实人像与项目截图后，重新校准背景安全区和实体对媒体主体的避让权重。
- 只有真实低端设备数据表明 Three.js WebGPU chunk 影响首屏后，才评估延迟启动或 Rust + wgpu/Wasm 替换。

## 2026-08-18 / v0.4 Browser QA

今天完成：

- 在独立 `test/v0.4-browser-qa` 分支复验桌面、移动、浅色、深色、WebGPU、WebGL2 和静态路径。
- 用干净浏览器标签页确认默认桌面背景为 `webgpu / full / ready`，控制台 warning/error 为空。
- 连续观察空闲场景，确认实体从初始 9 个增长至上限 14 个，破碎计数从 0 增长至 2。
- 复验移动菜单打开、Escape 关闭、项目锚点、牌组首次发牌和两张可见后牌。
- 发现移动主卡标题行存在 5px 内部横向越界，将标题行改为收缩安全的网格布局，并允许属性值自然换行。
- 逐张切换四张移动项目卡，确认卡内横向、纵向与页面级溢出均为 0。
- 复验工信部备案链接文本、目标地址、新窗口属性和页尾可见性。
- 将本轮桌面浅色、桌面深色、桌面牌组、移动首屏、移动菜单和移动牌组截图保存到忽略提交的 `artifacts/`。

当前现象：

- 默认桌面：`webgpu / full / ready`，14 个实体，至少 2 次空闲自然破碎。
- 默认移动：`webgpu / balanced / ready`，6 个初始实体，无页面水平溢出。
- 强制 WebGL2：`webgl2 / balanced / ready`，6 个初始实体。
- 强制静态：`static / static / ready`，Canvas 透明，八足 CSS 轮廓保留。
- 技术横条节点数为 0，重复证据侧板节点数为 0，卡面证据正文节点数为 1。
- 浅色基底为 `#f7f8f6`，动态实体和接触阴影清晰可辨。
- `bun run test`：5 passed、0 failed。
- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：静态生产构建成功。

下一步最小动作：

- 提交测试分支并以 `--no-ff` 合回 `develop`；`main` 保持当前发布门禁状态。

阻塞与证据：

- 无实现阻塞。真实个人资料与媒体资源仍待用户提供。

## 2026-08-19 / v0.5 Space Ocean

今天完成：

- 检索 WebGPU Samples 的 compute boids、Three.js Catmull-Rom 曲线与 MDN `backdrop-filter`，将采用范围和性能取舍写入 `docs/V0.5_SPACE_OCEAN_DESIGN.md`。
- 删除程序化八足生物、腿部动态缓冲、脚点实例和全部椭圆接触阴影。
- 将主生物改为由二次贝塞尔轮廓与三条弹簧曲线尾带组成的星鳐，游走仍受流场、边界、内容安全区、实体与鼠标斥力共同影响。
- 加入少量分离、对齐、聚合驱动的群游微生物；鼠标靠近时散开，离开后重新聚合。
- 将漂浮实体改为椭圆盘、胶囊和弯叶轮廓，保留碰撞、旋转、推动和有界破碎行为。
- 加入多条 Catmull-Rom 流线，并让像素粒子沿解析 curl 流场缓慢漂移。
- 将流场采样、群游 steering 和弹簧尾带推进抽为纯函数，单元测试从 5 个扩展到 8 个。
- 降低 Canvas 总透明度与对比度，用轻量雾化后处理隔离正文；项目卡和次按钮使用克制的磨砂表面，并提供 reduced-transparency 纯色回退。
- 浅色主题的 `--shadow` 设为透明，删除贴片感阴影；深色仍保留必要层级。
- 静态降级同步改为曲线流线、柔软实体和星鳐轮廓，不再出现蜘蛛形态。
- 接入真实 GitHub `https://github.com/xiaomo-xty` 与博客 `https://blog.littlesun.space`，简历和邮箱继续使用明确占位。

当前现象：

- 当前浏览器窄视口默认路径为 `webgpu / balanced / ready`，星鳐标记为 `space-ray`，10 个群游生物与 6 个初始实体正常初始化。
- 强制 WebGL2 为 `webgl2 / balanced / ready`，强制 static 为 `static / static / ready`。
- 静态路径包含 1 个星鳐轮廓和 3 条流线，旧 `.ambient-static-creature` 节点数量为 0。
- 浅色背景为 `#f7f8f6`，`--shadow` 为 `transparent`；深浅主题均无页面水平溢出。
- GitHub、博客和 ICP 备案链接均保留真实目标地址。
- `bun run test`：8 passed、0 failed。
- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：静态生产构建成功。

下一步最小动作：

- 将功能分支合入 `develop`，建立独立 `test/v0.5-browser-qa` 分支完成最终回归与 QA 记录。

阻塞与证据：

- 无实现阻塞。当前内置浏览器可视宽度为 731px，完整桌面宽屏截图需要在 QA 分支继续补充；姓名、人像、简历、邮箱和项目媒体仍待用户提供。

## 2026-08-19 / v0.6 Creature Concept Gate

今天完成：

- 用户否决直接实现的程序化鱼，要求恢复“概念图审核 -> 独立动画 Demo -> 主页集成”的三段式门禁。
- 将被否决的实现保存到 `feature/v0.6-procedural-fish` 对应 stash，主页工作区恢复到 `develop` 的 v0.5 状态。
- 在独立 `docs/v0.6-creature-concept` 分支制作第一版自创鱼类概念 `Lumen Drifter`，内部复核后判定不再推进。
- 按用户建议改用成熟生物原型，制作第二版 `Drift Companions`；用户指出自绘海豚短吻仍存在鸟喙感和吸嘴感，因此否决自绘海豚部分。
- 第三版直接采用 Twemoji `1F42C` 原始开源 SVG 作为海豚造型，概念板仅执行镜像和缩放；将 CC BY 4.0 来源、署名与后续修改说明保存在 `docs/concepts/vendor/TWEMOJI_ATTRIBUTION.md`。
- 将动画复杂度收敛为海豚 7 个控制点和水母 1 个钟罩弹簧值，避免再次依赖复杂的整身动态轮廓。
- 在 1280 x 720 浏览器视口完成概念板检查，完整页面宽 1265px、高 931px，无横向溢出，3 个 SVG 均正常渲染。

当前现象：

- 主页源码未修改，`develop` 仍保持 v0.5。
- `Concept 03` 仅处于静态审核状态，尚无动画 Demo；海豚资产实际加载成功，浏览器 console warning/error 为空。
- 概念板预览地址为 `http://127.0.0.1:4330/space-ocean-creatures-v2.html`。

下一步最小动作：

- 等待用户审核海豚与月水母的静态造型；通过后新建独立 Demo 分支，不直接进入主页。

阻塞与证据：

- 静态概念尚未获得用户确认，因此动画实现与主页集成均被门禁阻止。

## 2026-08-19 / v0.6 Creature Animation Demo

今天完成：

- 在 `feature/v0.6-creature-demo` 建立 `/labs/space-ocean-demo/` 独立路由，没有修改主页 `AmbientWorld.tsx`。
- 默认使用 Three.js `WebGPURenderer`，保留 `?backend=webgl2` 与 `?backend=static` 两条可重复验收路径。
- 发现 SVG 外部图像纹理在 WebGPU 中触发 `copyExternalImageToTexture` 失败，放弃纹理平面，改为解析 Twemoji SVG 路径并生成真实矢量网格。
- 仅对海豚主轮廓的尾部权重区域做顶点弯曲，眼睛、腹部、鳍和头部保持稳定。
- 将整身旋转改为左右翻身加小幅俯仰，避免垂直游动时出现侧翻怪感。
- 加入 3 只月水母、触手短链、解析流场、Catmull-Rom 流线和 124 个方形像素浮游物；平衡档使用 2 只水母与 72 个像素。
- 鼠标对海豚、水母和像素只产生斥力，点击产生一次扩散脉冲；增加暂停、重置与主题控制。
- 将浅色 Demo 基底从冰蓝灰调整为 `#f8faf8` 中性暖白。
- 补充静态兜底、reduced-motion、页面不可见暂停和透明度偏好降级。

当前现象：

- 桌面 1280 x 720 默认路径为 `webgpu / ready`，强制 WebGL2 为 `webgl2 / ready`，两条干净会话均无 console error/warning。
- 移动 390 x 844 深浅主题均无横向溢出、文字裁切或控件重叠。
- 海豚尾部在连续两帧中有明显形变，脸部和头部保持稳定；水母触手存在滞后，像素粒子持续闪烁和漂移。
- 鼠标靠近后生物远离指针，点击脉冲推开附近像素；暂停、主题和重置状态正确。
- 桌面与移动场景截图像素检查均为非空。
- `bun test`：8 passed、0 failed。
- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：静态生产构建成功。

下一步最小动作：

- 等待用户审核独立 Demo。通过后再新建主页集成分支；当前分支不合入 `develop` 或 `main`。

阻塞与证据：

- 门禁阻塞：Demo 尚未获得用户确认，主页集成不允许开始。
- Twemoji 署名与 CC BY 4.0 来源已保留；`D:\project\blog` 未修改。

## 2026-08-20 / v0.6 Creature Demo Rework Gate

今天完成：

- 复盘用户对第一版独立 Demo 的审核，确认问题不在后端兼容，而在海豚剪影、身体传导和掉头模型。
- 将自绘侧视稿 `space-ocean-creatures-v3` 标记为否决：头部、吻部、背线和尾柄比例仍不足以形成可信海豚。
- 将俯视探索 `space-ocean-dolphin-topdown-v4` 标记为未采用：它能解决镜像问题，但会损失用户认可的侧视形态。
- 确认第二版合同：成熟侧视海豚始终展示同一侧，整体航向在屏幕平面连续旋转，允许转圈、8 字、竖直和倒游。
- 采用 `argonautcode/animal-proc-anim` 的 MIT 链式约束思路，规划 8 节脊柱、固定链长、分段曲率限制和尾段推进波。
- 保留 WebGPU 优先、WebGL2 降级、静态兜底、鼠标斥力、短触手水母与像素浮游物。

当前现象：

- 当前分支仅允许修改独立 `/labs/space-ocean-demo/` 及其纯函数模拟模块和文档。
- `AmbientWorld.tsx` 保持不变，第二版 Demo 通过用户审核前不允许集成主页。
- 第一版运行时的 `SVGLoader`、速度符号镜像和随机目标瞬时切换已列入删除清单。

下一步最小动作：

- 先用单元测试锁定链长、关节角度和航向连续性，再重写独立 WebGPU Demo 并执行浏览器验收。

阻塞与证据：

- 无实现阻塞。主页集成仍受用户审核门禁约束。

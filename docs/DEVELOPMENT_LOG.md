# 开发日志

## 2026-08-16 / v0.3 Motion Foundation

今天完成：

- 新增全站章节进入与离开协议，使用 IntersectionObserver 反复触发，不监听滚动帧。
- 为 Hero、关于、技术带、项目、文章、生活与联系建立统一的可见状态。
- 加入 `420ms` 离开防抖，避免阈值附近滚动造成闪烁。
- 章节状态通过 `homepage:section-presence` 事件开放给后续 WebGPU 世界。
- reduced motion 或不支持 IntersectionObserver 时直接展示内容，不保留位移和透明度动画。

当前现象：

- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：成功。
- 本阶段没有修改项目数据、卡牌状态机或背景渲染器。

下一步最小动作：

- 创建项目侧板与卡牌编排分支，将问题、过程、证据同步到桌面侧板和移动摘要。

阻塞与证据：

- 无阻塞。

## 2026-08-16 / v0.3 Penpot V7

今天完成：

- 保留 Penpot V6 原稿，新建 V7 桌面主页、移动主页、交互基础、卡牌分镜和 WebGPU 动态世界说明五张画板。
- 将桌面项目空白区改为与当前卡同步的问题、过程、证据侧板，并增加紧凑牌组轨道。
- 为项目卡补充 GitHub、Demo 和更多证据的链接状态；移动端使用卡后证据摘要，避免把完整侧板压进窄屏。
- 以 `0 / 180 / 580 / 820ms` 四个状态记录旧牌回背、来牌翻转、粒子冲量和末端回弹。
- 记录 WebGPU → WebGL2 → 静态 CSS 降级路径、数据鳐行为、背景后处理限额和可重复章节动画。
- 导出五张 PNG 并检查文字裁切、元素重叠、对比度和移动端溢出；第二轮清理旧列表透出与翻面文案重叠。

当前现象：

- V7 设计已完成，等待确认；本阶段没有修改生产代码。
- 桌面与移动稿延续博客偏蓝配色，珊瑚红仅用于冲量、证据和注意力标记。
- Penpot HTTP MCP 可用，插件面板需保持打开；长画板导出耗时约 30–80 秒。

下一步最小动作：

- 用户确认 V7 后，从 `develop` 创建 `feature/motion-foundation`，先实现统一 motion 协议和 reduced-motion 基线。

阻塞与证据：

- 无技术阻塞；生产实现受设计确认门禁约束。

新增 Backlog：

- 用真实 SceneScope 仓库和演示 URL 替换设计中的链接占位。
- 生产实现时用 WebGPU capability 标记验证实际 backend，不只检测 API 是否存在。

## 2026-08-16 / Repository

今天完成：

- 确认 Penpot V6 为实现基线。
- 完整阅读职业发展文件，提取内容边界。
- 只读审计现有博客的蓝色主题、面板层次与主题切换方式。
- 初始化独立 Git 仓库并定义分支流程。

当前现象：

- `D:\project\homepage` 是新仓库，尚未建立 Astro 工程。
- 姓名、人像、简历、GitHub 和邮箱均未提供。
- 文章题目来自职业规划中的候选方向，尚未确认真实发布状态。

下一步最小动作：

- 从 `develop` 创建 `feature/foundation`，建立可运行的 Astro + Bun 骨架和主题变量。

阻塞与证据：

- 无阻塞。

新增 Backlog：

- 用真实资料替换个人占位符。
- 用真实 SceneScope 截图替换项目截图槽位。
- 接入已发布文章的真实链接和发布日期。

## 2026-08-16 / Foundation

今天完成：

- 建立 Astro 5 + Bun 工程与 React island 支持。
- 建立浅色、深色语义变量和系统主题首屏初始化。
- 自托管中文、拉丁与局部像素字体。
- 建立项目、文章和站点信息的数据模型。
- 完成语义页面骨架、SEO 基线和明确个人资料占位符。

当前现象：

- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：静态首页构建成功。
- 依赖安装首次遇到注册表连接拒绝，切换镜像后完成并生成 `bun.lock`。

下一步最小动作：

- 将静态骨架重构为 V6 叙事布局，完成响应式导航、关于我、文章、生活和联系区域。

阻塞与证据：

- 无阻塞。

新增 Backlog：

- 增加可访问的主题切换控件。
- 将作品列表改造成可扩展卡组，不把数量写死在视图层。

## 2026-08-16 / Narrative

今天完成：

- 按 Penpot V6 重构首屏、关于我、作品、文章、生活与联系区域。
- 建立人物素材、项目截图、文章缩略图和生活照片的明确占位区域。
- 增加浅色与深色主题切换、移动端全屏导航和 Escape 关闭行为。
- 使用像素字体、像素角标、空间轴与叠层表达技术气质，不使用受版权保护的角色素材。
- 作品数据仍由数组驱动，页面没有写死三张卡牌的上限。

当前现象：

- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：静态首页与 React 导航 island 构建成功。
- 当前作品卡是静态首张状态，按钮行为将在交互阶段接入。

下一步最小动作：

- 在独立交互分支实现卡牌切换、翻面、触摸滑动和键盘操作。

阻塞与证据：

- 无阻塞。

新增 Backlog：

- 为首屏加入自适应 Three.js 世界背景。
- 为类型状态加入克制的打字机效果。
- 为卡牌切换加入像素粒子反馈和 reduced-motion 降级。

## 2026-08-16 / Interaction

今天完成：

- 用 Three.js 建立铺满首屏的实时世界背景，并按设备能力调整像素粒子数量和 DPR。
- 在页面不可见、标签页隐藏或系统要求减少动态效果时暂停或降级动画。
- 为职业方向加入打字机状态行，保留静态文本作为无脚本和 reduced-motion 回退。
- 将代表项目改造成数据驱动的可扩展卡组，项目数量不受视图层限制。
- 完成分类筛选、循环切牌、卡牌翻面、指针倾斜、触摸滑动和键盘操作。
- 卡牌背面统一呈现问题、过程与证据，切换时加入克制的像素粒子反馈。

当前现象：

- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：静态构建成功，Three.js、Motion 与 React islands 正常产出。
- WebGL 不可用时保留静态背景；交互控件具备可见焦点和语义标签。

下一步最小动作：

- 从 `develop` 创建独立测试分支，记录桌面、移动端、主题、交互和画布验收结果。

阻塞与证据：

- 无阻塞。

新增 Backlog：

- 用真实项目截图和可公开的商业开发证据替换占位内容。
- 根据正式内容长度复核卡牌背面排版与筛选分类。
- 后续评估是否用 wgpu/Wasm 替换首屏渲染实验，但不纳入首版范围。

## 2026-08-16 / Browser QA

今天完成：

- 在 1440 x 900 桌面视口和 390 x 844 移动视口完成深浅主题视觉验收。
- 验证移动菜单、项目筛选、循环切牌、翻面、触摸滑动与键盘操作。
- 检查文字裁切、元素重叠、水平溢出、颜色对比度和首屏章节提示。
- 验证 Three.js 画布非空、可见性暂停、reduced-motion 和静态背景降级。
- 将验收矩阵、数值证据和内容边界写入 `docs/QA_REPORT.md`。

当前现象：

- 浏览器控制台无 warning 或 error。
- 桌面与移动端均无水平溢出；关键交互均通过鼠标、触摸或键盘验证。
- 姓名、联系信息、项目图片与文章仍是明确占位内容，不影响原型验收。

下一步最小动作：

- 在 `develop` 重新运行完整检查，合入 `main` 并标记 `v0.1.0`。

阻塞与证据：

- 无阻塞。

新增 Backlog：

- 获得真实个人资料后建立独立内容分支替换占位符。
- 接入正式域名之前补充站点图标、Open Graph 图片和部署配置。

## 2026-08-16 / Card Deck Motion

今天完成：

- 修正正反面在指针倾斜状态下产生不同透视位置的问题，翻面前统一归零外层倾斜。
- 将正反面改为同一固定卡高和同一套五行网格，标题、内容区、技术栈与翻面按钮共享空间基准。
- 移除串行等待切牌，改为旧牌退场与新牌从后方上桌同时发生的 spring 旋转过渡。
- 用两张真实项目预览替换装饰性卡片阴影，只渲染当前牌和后两张，数据规模仍不受限制。
- 后牌支持点击与键盘切换，并保留方向键、翻面键和移动端拖动操作。
- 修复旋转后的后牌覆盖下方切牌按钮、导致一次点击跳过两张的问题。
- 翻面后将键盘焦点移动到当前可见面的控制按钮。

当前现象：

- 桌面端正反面边界均为 `520 x 650`，坐标完全一致。
- 移动端正反面边界均为 `321 x 600`，四张项目卡的正面和证据区均无内容溢出。
- 1440 x 900 与 390 x 844 视口均无页面水平溢出。
- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：生产构建成功。

下一步最小动作：

- 在独立测试分支记录卡组补丁验收结果，合入 `develop` 后发布 `v0.1.1`。

阻塞与证据：

- 无阻塞。

新增 Backlog：

- 接入真实项目图片后复核不同图片明暗对卡牌空间层级的影响。

## 2026-08-16 / Playable Project Cards

今天完成：

- 将切牌改造成明确的五段状态：盖回卡背、收回牌堆、抽取新牌、翻到正面、恢复可操作。
- 首次进入项目区时按远牌、近牌、主牌的顺序逐张发牌，主牌先展示装饰卡背再翻正。
- 卡背只保留像素网格、几何轨道和牌组核心纹样，不再承载项目信息。
- 卡面加入作品图槽、真实语义的属性词条、技术标签、项目动作和正面详情层。
- 项目链接升级为可扩展 `links[]`；资料没有提供真实 URL 时显示“公开链接待补充”，不生成虚假外链。
- 新增全页 Canvas 环境层，以固定种子生成像素和简单几何，并支持鼠标视差与近距离粒子响应。
- 背景在无鼠标输入时也会独立改变位置、大小、亮度和轮廓，变化使用随机目标与缓动，不产生逐帧噪声抖动。
- 为减少动态、省流量、移动端和低核心数设备保留相应降级路径，并在标签页隐藏时暂停动画。

当前现象：

- 桌面卡面为 `520 x 690`，移动端约为 `321 x 650`，四张项目卡和详情层均无内容溢出。
- 单次切牌约 `1.16s`，过渡期间锁定输入，因此一次点击或按键只抽取一张牌。
- 桌面悬停、键盘聚焦后的按钮操作和移动端点击均可打开详情。
- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：静态生产构建成功。

下一步最小动作：

- 从功能分支合入 `develop`，在独立测试分支记录完整验收后发布 `v0.2.0`。

阻塞与证据：

- SceneScope 仓库、Demo 和案例文章 URL 尚未提供，因此项目动作保持明确占位状态。

新增 Backlog：

- 获得真实链接后只修改 `src/data/projects.ts`，不改卡牌组件结构。
- 接入真实项目截图后复核详情浮层与图片焦点区域的关系。

## 2026-08-16 / v0.3 Interaction Design Planning

今天完成：

- 审计 v0.2.0 的卡牌定时器状态机、一次性发牌触发、双背景实现和项目区信息分配。
- 确认卡顿来自固定等待、组件重挂载、弹簧收敛和层级切换叠加，不把问题简化为单一缓动曲线。
- 将下一版统一为“活体技术世界”方向，并定义信息层、交互层和环境层的动态预算。
- 规划可重复章节入退场、固定槽位卡牌时序、项目牌组侧板、程序化数据鳐和背景专用后处理。
- 根据新增约束将背景确定为 WebGPU 优先、WebGL2 降级、静态 CSS 兜底，并确认当前 Three.js 版本具备对应 renderer、fallback backend 和节点后处理能力。
- 将唯一推荐方案、实施分支和验收标准写入 `docs/V0.3_INTERACTION_DESIGN.md`。

当前现象：

- 本阶段只修改文档，没有改动页面实现、依赖或博客项目。
- 完整方案明显超过一次小修范围，拆分为 Motion、项目侧板、WebGPU 动态世界、后处理和浏览器验收五个阶段。

下一步最小动作：

- 等待方案确认；确认后从更新后的 `develop` 创建 `feature/motion-foundation`，只实施第一阶段。

阻塞与证据：

- 无技术阻塞；实现开始前需要用户确认 v0.3 方案。

新增 Backlog：

- 后续获得真实项目链接与图像时，在独立内容分支替换占位内容。
- v0.3 验收时分别记录 WebGPU 主路径和 WebGL2 fallback 的运行证据。

## 2026-08-16 / v0.3 Motion Foundation

今天完成：

- 建立可重复触发的章节进入与离开协议，Hero、关于、技术带、项目、文章、生活和联系区均已接入。
- 使用 IntersectionObserver 与 420ms 离开迟滞，避免章节边界附近反复闪烁。
- 章节重新进入时保留主题、项目筛选和当前卡等交互状态。
- 向背景层派发 `homepage:section-presence` 事件，为统一动态世界提供章节状态桥接。
- `prefers-reduced-motion` 下取消位移与裁切，直接显示稳定内容。

当前现象：

- 章节从上方或下方重新进入均可再次播放，滚动过程不被劫持。
- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：静态生产构建成功。

下一步最小动作：

- 在独立分支完成项目牌组侧板与重叠切牌时序。

阻塞与证据：

- 无阻塞。

## 2026-08-16 / v0.3 Project Sideboard

今天完成：

- 将固定定时器切牌重构为 Motion animation sequence，以重叠时间线完成旧牌翻背、回堆、新牌抽取和翻正。
- 单次切牌总时长收敛到约 820ms，出牌和来牌作为同时存在的独立实体参与过渡。
- 项目区重新进入视口时重新发出当前可见牌组，但保留筛选和当前项目。
- 桌面左侧加入与当前卡同步的 Problem / Process / Evidence 证据侧板和可扩展牌轨。
- 移动端加入卡后证据摘要；真实 URL 仍是项目动作出现的唯一条件。
- 为每个过渡卡实例生成唯一详情标识，并让隐藏卡面退出键盘交互。
- 同项目筛选直接更新牌组范围，不播放“同一张牌翻到自己”的无意义动画。

当前现象：

- 1440 x 1000 桌面项目区的侧板、当前牌和后两张牌均保持完整层次。
- 390 x 844 移动视口无水平溢出，卡牌与证据摘要均处于页面安全宽度内。
- 浏览器复验中，切牌过渡没有重复 DOM `id`，隐藏卡面内可聚焦元素为 0。
- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：静态生产构建成功。

下一步最小动作：

- 合入 `develop`，从更新后的集成分支创建 `feature/webgpu-ambient-world`。

阻塞与证据：

- SceneScope 仓库、Demo、简历和联系方式尚未提供，相关动作继续保留明确占位。

## 2026-08-16 / v0.3 WebGPU Ambient World

今天完成：

- 删除 Hero WebGL 与全页 Canvas 2D 两套旧背景，合并为单一固定 `AmbientWorld` 场景。
- 使用 Three.js `WebGPURenderer` 作为主路径；无 WebGPU 时进入 WebGL2 backend，初始化失败或主动降级时保留静态 CSS 几何。
- 通过 `data-backend="webgpu|webgl2|static"`、`data-quality` 和 `data-status` 暴露可验收运行状态。
- 在同一场景中加入低面几何、像素粒子、指针吸引与排斥、点击脉冲、抽牌冲量和抽象数据鳐。
- 数据鳐使用游走、指针场、几何避障、内容安全区排斥与边界力，不引用受版权保护的角色形象。
- 背景监听 `homepage:section-presence`，在 Hero、关于、技术带、项目、文章、生活和联系区之间平滑切换能量参数。
- 增加只覆盖画布的轻量扫描、暗角和色调统一层，不对 DOM 文本、图片或按钮做后处理。
- 限制桌面与移动 DPR、粒子数量和后端质量；页面隐藏时暂停，恢复时重置帧间隔。
- 将项目章节 reveal 属性移到 Astro 外层，消除 `client:visible` React 岛的 hydration mismatch。
- 保留 `?ambient=webgl2` 与 `?ambient=static` 作为本地强制验收入口。

当前现象：

- 默认路径实测为 `webgpu / full / ready`；强制降级实测为 `webgl2 / balanced / ready` 和 `static / static / ready`。
- 1440 x 900 桌面与 390 x 844 移动视口无水平溢出，桌面和移动 Hero 均在首屏保留下一章节提示。
- 移动项目摘要可展开，卡片 `scrollWidth === clientWidth` 且 `scrollHeight === clientHeight`。
- WebGPU 局部截图在无指针输入的 1.2 秒内哈希变化，证明空闲几何、粒子和数据鳐持续更新。
- WebGPU、WebGL2、静态路径及深浅主题复验后，控制台 warning/error 均为空。
- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：静态生产构建成功，无构建告警。
- WebGPU 场景 chunk 为 687.81 kB minified / 191.12 kB gzip，这是当前 Three.js WebGPU renderer 的主要首屏成本。

下一步最小动作：

- 合入 `develop`，建立 `test/v0.3-browser-qa` 完成最终回归并更新 `docs/QA_REPORT.md`。

阻塞与证据：

- 无功能阻塞。真实照片、项目截图和对外链接仍需用户提供。

新增 Backlog：

- 获得真实媒体后复核背景安全区和照片对比度。
- 若实机低端设备数据证明 191.12 kB gzip 仍影响首屏，再评估延迟启动或 Rust + wgpu/Wasm 替换，不在无数据时提前扩展架构。
- 高级 TSL selective bloom 继续保持关闭；只有当前轻量后处理不足且性能预算允许时才单独加入。

## 2026-08-16 / v0.3 Browser QA

今天完成：

- 在独立 `test/v0.3-browser-qa` 分支完成桌面与移动端、浅色与深色的浏览器回归。
- 复验移动菜单的打开、Escape 关闭和锚点导航关闭行为。
- 复验项目牌组的键盘切牌、详情展开、唯一 DOM 标识和移动端内容边界。
- 让项目区离开视口后重新进入，确认发牌周期再次触发，同时保留当前卡与主题。
- 分别验证 WebGPU、强制 WebGL2 和静态降级路径，三条干净加载会话均无 warning/error。
- 检查可见文案、页面水平溢出以及移动卡面内部溢出。

当前现象：

- 桌面 1440 x 900 与移动 390 x 844 均无水平溢出，浅色和深色主题正常。
- 默认桌面为 `webgpu / full / ready`，默认移动为 `webgpu / balanced / ready`。
- 强制后端分别为 `webgl2 / balanced / ready` 与 `static / static / ready`。
- 项目重入后发牌周期从 `1` 递增到 `2`，商业 C++ 当前卡和深色主题保持不变。
- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：静态生产构建成功，无构建告警。

下一步最小动作：

- 将测试分支以 `--no-ff` 合并回 `develop`，保留 `main` 作为后续正式发布门禁。

阻塞与证据：

- 无实现阻塞。姓名、照片、简历、联系方式、项目媒体和真实外链仍待用户提供。

## 2026-08-18 / v0.4 Emergent Ambient

今天完成：

- 保留 DeepSeek 已加入的 MIT License 与 ICP 备案信息，并将对应 `main` 提交同步回 `develop` 后再创建本功能分支。
- 删除技术关键词横条，不再用重复技术名词打断个人叙事。
- 将项目左侧 Problem / Process / Evidence 证据侧板收敛为可扩展牌组索引，详情正文只保留在卡面的一处内容源。
- 将浅色主题从冷灰蓝调整为中性瓷白，同时为动态实体增加可辨识填充、描边和接触阴影。
- 将背景对象拆分为不参与碰撞的像素特效、可碰撞几何实体和程序化八足生物。
- 加入包围圆碰撞、速度交换、旋转与命中反馈，以及圆形、长方形、三角形的有界破碎规则。
- 生物行为由游走目标、附近实体、内容安全区、边界和指针斥力共同驱动，可以踢开实体并触发破碎。
- 鼠标对实体和生物只产生斥力；快速移动和点击脉冲提高弹开冲量，不把背景对象吸向指针。
- 将碰撞、鼠标斥力、破碎模式和实体上限抽到纯函数模块，并新增 5 个 Bun 单元测试。
- 修正 WebGL2 质量标记与真实资源预算不一致的问题；WebGL2 现在实际使用移动级粒子数、实体数、DPR 和抗锯齿配置。
- 背景 `data-status` 改为首帧仅写一次，避免在渲染循环中重复更新同一个 DOM 属性。

当前现象：

- 桌面默认路径实测为 `webgpu / full / ready`，实体数稳定在上限 14，破碎计数能在无输入时自然增长。
- 移动默认路径实测为 `webgpu / balanced / ready`，初始实体数为 6，页面无水平溢出。
- 强制路径实测为 `webgl2 / balanced / ready` 和 `static / static / ready`，两者均无控制台 warning/error。
- 浅色页面基底为 `#f7f8f6`，动态实体在浅色和深色中均可辨识。
- 项目切牌实测约 0.9 秒，旧牌翻回卡背与新牌由卡背翻正均在过渡中可见。
- 项目区离开后再次进入，发牌周期从 `1` 增加到 `2`；牌组状态和主题保持不变。
- ICP 备案链接实测为 `湘ICP备2026030115号-1`，目标为工信部备案系统。
- `bun run test`：5 passed、0 failed。
- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：静态生产构建成功。

下一步最小动作：

- 将功能分支以 `--no-ff` 合入 `develop`，再建立独立 `test/v0.4-browser-qa` 分支记录最终回归。

阻塞与证据：

- 无实现阻塞。姓名、人像、简历、联系方式、项目媒体与真实外链仍待用户提供。

新增 Backlog：

- 获得真实人像与项目截图后，重新校准背景安全区和实体对媒体主体的避让权重。
- 只有真实低端设备数据表明 Three.js WebGPU chunk 影响首屏后，才评估延迟启动或 Rust + wgpu/Wasm 替换。

## 2026-08-18 / v0.4 Browser QA

今天完成：

- 在独立 `test/v0.4-browser-qa` 分支复验桌面、移动、浅色、深色、WebGPU、WebGL2 和静态路径。
- 用干净浏览器标签页确认默认桌面背景为 `webgpu / full / ready`，控制台 warning/error 为空。
- 连续观察空闲场景，确认实体从初始 9 个增长至上限 14 个，破碎计数从 0 增长至 2。
- 复验移动菜单打开、Escape 关闭、项目锚点、牌组首次发牌和两张可见后牌。
- 发现移动主卡标题行存在 5px 内部横向越界，将标题行改为收缩安全的网格布局，并允许属性值自然换行。
- 逐张切换四张移动项目卡，确认卡内横向、纵向与页面级溢出均为 0。
- 复验工信部备案链接文本、目标地址、新窗口属性和页尾可见性。
- 将本轮桌面浅色、桌面深色、桌面牌组、移动首屏、移动菜单和移动牌组截图保存到忽略提交的 `artifacts/`。

当前现象：

- 默认桌面：`webgpu / full / ready`，14 个实体，至少 2 次空闲自然破碎。
- 默认移动：`webgpu / balanced / ready`，6 个初始实体，无页面水平溢出。
- 强制 WebGL2：`webgl2 / balanced / ready`，6 个初始实体。
- 强制静态：`static / static / ready`，Canvas 透明，八足 CSS 轮廓保留。
- 技术横条节点数为 0，重复证据侧板节点数为 0，卡面证据正文节点数为 1。
- 浅色基底为 `#f7f8f6`，动态实体和接触阴影清晰可辨。
- `bun run test`：5 passed、0 failed。
- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：静态生产构建成功。

下一步最小动作：

- 提交测试分支并以 `--no-ff` 合回 `develop`；`main` 保持当前发布门禁状态。

阻塞与证据：

- 无实现阻塞。真实个人资料与媒体资源仍待用户提供。

## 2026-08-19 / v0.5 Space Ocean

今天完成：

- 检索 WebGPU Samples 的 compute boids、Three.js Catmull-Rom 曲线与 MDN `backdrop-filter`，将采用范围和性能取舍写入 `docs/V0.5_SPACE_OCEAN_DESIGN.md`。
- 删除程序化八足生物、腿部动态缓冲、脚点实例和全部椭圆接触阴影。
- 将主生物改为由二次贝塞尔轮廓与三条弹簧曲线尾带组成的星鳐，游走仍受流场、边界、内容安全区、实体与鼠标斥力共同影响。
- 加入少量分离、对齐、聚合驱动的群游微生物；鼠标靠近时散开，离开后重新聚合。
- 将漂浮实体改为椭圆盘、胶囊和弯叶轮廓，保留碰撞、旋转、推动和有界破碎行为。
- 加入多条 Catmull-Rom 流线，并让像素粒子沿解析 curl 流场缓慢漂移。
- 将流场采样、群游 steering 和弹簧尾带推进抽为纯函数，单元测试从 5 个扩展到 8 个。
- 降低 Canvas 总透明度与对比度，用轻量雾化后处理隔离正文；项目卡和次按钮使用克制的磨砂表面，并提供 reduced-transparency 纯色回退。
- 浅色主题的 `--shadow` 设为透明，删除贴片感阴影；深色仍保留必要层级。
- 静态降级同步改为曲线流线、柔软实体和星鳐轮廓，不再出现蜘蛛形态。
- 接入真实 GitHub `https://github.com/xiaomo-xty` 与博客 `https://blog.littlesun.space`，简历和邮箱继续使用明确占位。

当前现象：

- 当前浏览器窄视口默认路径为 `webgpu / balanced / ready`，星鳐标记为 `space-ray`，10 个群游生物与 6 个初始实体正常初始化。
- 强制 WebGL2 为 `webgl2 / balanced / ready`，强制 static 为 `static / static / ready`。
- 静态路径包含 1 个星鳐轮廓和 3 条流线，旧 `.ambient-static-creature` 节点数量为 0。
- 浅色背景为 `#f7f8f6`，`--shadow` 为 `transparent`；深浅主题均无页面水平溢出。
- GitHub、博客和 ICP 备案链接均保留真实目标地址。
- `bun run test`：8 passed、0 failed。
- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：静态生产构建成功。

下一步最小动作：

- 将功能分支合入 `develop`，建立独立 `test/v0.5-browser-qa` 分支完成最终回归与 QA 记录。

阻塞与证据：

- 无实现阻塞。当前内置浏览器可视宽度为 731px，完整桌面宽屏截图需要在 QA 分支继续补充；姓名、人像、简历、邮箱和项目媒体仍待用户提供。

## 2026-08-19 / v0.6 Creature Concept Gate

今天完成：

- 用户否决直接实现的程序化鱼，要求恢复“概念图审核 -> 独立动画 Demo -> 主页集成”的三段式门禁。
- 将被否决的实现保存到 `feature/v0.6-procedural-fish` 对应 stash，主页工作区恢复到 `develop` 的 v0.5 状态。
- 在独立 `docs/v0.6-creature-concept` 分支制作第一版自创鱼类概念 `Lumen Drifter`，内部复核后判定不再推进。
- 按用户建议改用成熟生物原型，制作第二版 `Drift Companions`；用户指出自绘海豚短吻仍存在鸟喙感和吸嘴感，因此否决自绘海豚部分。
- 第三版直接采用 Twemoji `1F42C` 原始开源 SVG 作为海豚造型，概念板仅执行镜像和缩放；将 CC BY 4.0 来源、署名与后续修改说明保存在 `docs/concepts/vendor/TWEMOJI_ATTRIBUTION.md`。
- 将动画复杂度收敛为海豚 7 个控制点和水母 1 个钟罩弹簧值，避免再次依赖复杂的整身动态轮廓。
- 在 1280 x 720 浏览器视口完成概念板检查，完整页面宽 1265px、高 931px，无横向溢出，3 个 SVG 均正常渲染。

当前现象：

- 主页源码未修改，`develop` 仍保持 v0.5。
- `Concept 03` 仅处于静态审核状态，尚无动画 Demo；海豚资产实际加载成功，浏览器 console warning/error 为空。
- 概念板预览地址为 `http://127.0.0.1:4330/space-ocean-creatures-v2.html`。

下一步最小动作：

- 等待用户审核海豚与月水母的静态造型；通过后新建独立 Demo 分支，不直接进入主页。

阻塞与证据：

- 静态概念尚未获得用户确认，因此动画实现与主页集成均被门禁阻止。

## 2026-08-19 / v0.6 Creature Animation Demo

今天完成：

- 在 `feature/v0.6-creature-demo` 建立 `/labs/space-ocean-demo/` 独立路由，没有修改主页 `AmbientWorld.tsx`。
- 默认使用 Three.js `WebGPURenderer`，保留 `?backend=webgl2` 与 `?backend=static` 两条可重复验收路径。
- 发现 SVG 外部图像纹理在 WebGPU 中触发 `copyExternalImageToTexture` 失败，放弃纹理平面，改为解析 Twemoji SVG 路径并生成真实矢量网格。
- 仅对海豚主轮廓的尾部权重区域做顶点弯曲，眼睛、腹部、鳍和头部保持稳定。
- 将整身旋转改为左右翻身加小幅俯仰，避免垂直游动时出现侧翻怪感。
- 加入 3 只月水母、触手短链、解析流场、Catmull-Rom 流线和 124 个方形像素浮游物；平衡档使用 2 只水母与 72 个像素。
- 鼠标对海豚、水母和像素只产生斥力，点击产生一次扩散脉冲；增加暂停、重置与主题控制。
- 将浅色 Demo 基底从冰蓝灰调整为 `#f8faf8` 中性暖白。
- 补充静态兜底、reduced-motion、页面不可见暂停和透明度偏好降级。

当前现象：

- 桌面 1280 x 720 默认路径为 `webgpu / ready`，强制 WebGL2 为 `webgl2 / ready`，两条干净会话均无 console error/warning。
- 移动 390 x 844 深浅主题均无横向溢出、文字裁切或控件重叠。
- 海豚尾部在连续两帧中有明显形变，脸部和头部保持稳定；水母触手存在滞后，像素粒子持续闪烁和漂移。
- 鼠标靠近后生物远离指针，点击脉冲推开附近像素；暂停、主题和重置状态正确。
- 桌面与移动场景截图像素检查均为非空。
- `bun test`：8 passed、0 failed。
- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：静态生产构建成功。

下一步最小动作：

- 等待用户审核独立 Demo。通过后再新建主页集成分支；当前分支不合入 `develop` 或 `main`。

阻塞与证据：

- 门禁阻塞：Demo 尚未获得用户确认，主页集成不允许开始。
- Twemoji 署名与 CC BY 4.0 来源已保留；`D:\project\blog` 未修改。

## 2026-08-20 / v0.6 Creature Demo Rework Gate

今天完成：

- 复盘用户对第一版独立 Demo 的审核，确认问题不在后端兼容，而在海豚剪影、身体传导和掉头模型。
- 将自绘侧视稿 `space-ocean-creatures-v3` 标记为否决：头部、吻部、背线和尾柄比例仍不足以形成可信海豚。
- 将俯视探索 `space-ocean-dolphin-topdown-v4` 标记为未采用：它能解决镜像问题，但会损失用户认可的侧视形态。
- 确认第二版合同：成熟侧视海豚始终展示同一侧，整体航向在屏幕平面连续旋转，允许转圈、8 字、竖直和倒游。
- 采用 `argonautcode/animal-proc-anim` 的 MIT 链式约束思路，规划 8 节脊柱、固定链长、分段曲率限制和尾段推进波。
- 保留 WebGPU 优先、WebGL2 降级、静态兜底、鼠标斥力、短触手水母与像素浮游物。

当前现象：

- 当前分支仅允许修改独立 `/labs/space-ocean-demo/` 及其纯函数模拟模块和文档。
- `AmbientWorld.tsx` 保持不变，第二版 Demo 通过用户审核前不允许集成主页。
- 第一版运行时的 `SVGLoader`、速度符号镜像和随机目标瞬时切换已列入删除清单。

下一步最小动作：

- 先用单元测试锁定链长、关节角度和航向连续性，再重写独立 WebGPU Demo 并执行浏览器验收。

阻塞与证据：

- 无实现阻塞。主页集成仍受用户审核门禁约束。

## 2026-08-20 / v0.6 Creature Demo Rework Implementation

今天完成：

- 新增 `dolphinSimulation.ts`，实现固定链长脊柱、分段曲率限制、最短角航向插值、8 字和环游路径，以及带阻尼的鼠标斥力偏移。
- 新增 4 个海豚模拟测试，锁定链节长度、关节角度、航向跨界连续性与非镜像转向。
- 删除独立 Demo 的 `SVGLoader` 和 Twemoji 运行时依赖，移除由水平速度符号驱动的 `scaleX` 翻面。
- 用统一脊柱驱动程序化身体、腹部、背鳍、胸鳍、尾鳍、轮廓、眼睛与嘴线；每帧只更新现有顶点，不重建几何拓扑。
- 提供 8 字与环游两种可切换路径，缩短完整转向观察周期，并把路径活动中心移出标题安全区。
- 将水母触手改为组内局部短链；回绕时重置局部链，消除跨屏长线。
- 保留 124 / 72 个方形像素浮游物、Catmull-Rom 流线、鼠标斥力和点击扩散脉冲。
- 将静态兜底同步为本版侧视轮廓，包含腹部、胸鳍、眼睛与嘴线，不再依赖 Twemoji 资源。
- 浏览器验收发现 `THREE.LineLoop` 在统一 WebGPU 渲染器中不受支持，改为首尾点重复的 `THREE.Line` 后消除逐帧错误。

当前现象：

- 桌面 1280 x 720 的 WebGPU 与强制 WebGL2 均为 `ready`，干净会话无 console warning/error，页面横向溢出为 0。
- 移动 390 x 844 的深浅主题均无标题、海豚、控制条和状态栏重叠，页面横向溢出为 0。
- 静态路径为 `static / fallback`，程序化侧视占位可见，干净会话无 warning/error。
- 路径切换、暂停、继续、重置和主题切换状态正确；海豚始终标记为 `same-side`。
- `bun test`：12 passed、0 failed。
- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：2 个静态路由构建成功。

下一步最小动作：

- 等待用户审核独立 Demo 的造型和运动。通过后再创建主页集成分支；当前分支不得直接合入 `develop`。

阻塞与证据：

- 门禁阻塞：第二版 Demo 尚未获得用户确认。
- `AmbientWorld.tsx` 和 `D:\project\blog` 均未修改。

## 2026-08-20 / v0.6 Creature Figure-eight Follow-up

今天完成：

- 根据用户指出的“头固定、身体绕头旋转”问题，移除脊柱求解器对路径目标航向的直接依赖。
- 将 8 字路径改为近似恒定弧长推进，避免参数曲线在不同曲率区域出现忽快忽慢。
- 路径只更新头部位置；第一节身体由上一帧世界位置被动拖动，转向再沿固定长度关节逐节传到尾部。
- 将路径相位与尾部摆动时钟拆开，保证恒速路线和推进波各自连续。
- 粒子尾流改用脊柱第一节的真实切线，不再引用已经删除的全局航向变量。
- 新增两项回归测试，分别锁定 8 字头部恒速前进和第一节身体不能瞬时绕头对齐。

当前现象：

- 8 字路径的单帧头部位移稳定在约 `0.016`，外侧弯点不会停住后原地转身。
- 头部转向时胸段保留空间惯性，弯曲由前向后传播，尾段只叠加较弱推进波。
- `bun test`：14 passed、0 failed。

下一步最小动作：

- 由用户在独立 Demo 中观察完整 8 字周期；通过后再补主页集成分支，当前分支仍不合入 `develop`。

阻塞与证据：

- 当前内置浏览器对本地 `127.0.0.1` 预览的自动接管被 URL 安全策略拒绝，因此本轮无法补自动截图；本地开发服务器与用户可见预览仍正常提供。
- `AmbientWorld.tsx` 和 `D:\project\blog` 均未修改。

## 2026-08-20 / v0.6 Creature Demo Runtime Recovery

今天完成：

- 排查用户反馈的 Demo 完全不动，确认开发服务器持续返回 `200`，问题位于客户端动画状态机。
- 调整动画帧循环：先安排下一帧，再读取当前 `document.hidden`；后台初始化或可见性事件丢失不再永久终止循环。
- 修正 reduced-motion 与控制条状态不一致的问题；系统要求减少动态时现在初始显示为暂停，用户点击播放可明确启用动画。
- 新增 `?motion=full` 预览参数，用于对运动研究页面进行完整动画验收，同时保留默认无障碍行为。
- 暂停与重置操作同步写入运行时引用，消除 React 状态提交前的一帧控制延迟。

当前现象：

- 页面在后台标签初始化后切回前台，动画循环能够恢复。
- reduced-motion 环境不再出现“按钮显示正在播放但模拟静止”的假状态。

下一步最小动作：

- 使用 `/labs/space-ocean-demo/?motion=full` 复验 8 字完整周期，再由用户判断身体传导观感。

阻塞与证据：

- `AmbientWorld.tsx` 和 `D:\project\blog` 均未修改。

## 2026-08-21 / v0.6 Renderer Initialization Fallback

今天完成：

- 根据用户截图确认 Demo 停在 `INITIALIZING / LOADING`，动画模拟尚未开始，排除路径和骨架更新问题。
- 为 Three.js WebGPU renderer 初始化增加 5 秒上限，避免浏览器声明 WebGPU 后在设备或适配器申请阶段无限等待。
- WebGPU 初始化超时或失败时自动创建 WebGL2 renderer；仅当 WebGL2 同样失败时进入静态兜底。
- 自动降级后同步启用平衡档 DPR、抗锯齿和场景资源预算，并通过 `data-fallback-reason="webgpu-init"` 暴露原因。

当前现象：

- 开发服务器持续监听 `127.0.0.1:4333`，页面请求正常；此前永久 loading 的客户端路径已有确定退出条件。

下一步最小动作：

- 刷新 `/labs/space-ocean-demo/?motion=full`，确认状态在 5 秒内进入 `webgpu / ready` 或 `webgl2 / ready`。

阻塞与证据：

- `AmbientWorld.tsx` 和 `D:\project\blog` 均未修改。

## 2026-08-21 / v0.6 Runtime Diagnostics

今天完成：

- 根据用户要求为独立 Demo 增加 `?debug=1` 诊断模式，不影响主页和普通预览。
- 日志覆盖启动环境、动画偏好、渲染器初始化开始/成功/超时/异常、WebGL2 降级、场景构建、首帧和两秒心跳。
- 将诊断日志同时写入控制台和页面内可视面板，便于无法打开开发者工具时直接截图反馈。
- 捕获原先未处理的场景构建异步异常和渲染循环异常；出错时显示错误原文并进入静态兜底，不再停留在 loading。

当前现象：

- 通过 `/labs/space-ocean-demo/?motion=full&debug=1` 可以直接确定冻结发生在 renderer、scene 还是 frame 阶段。

下一步最小动作：

- 由用户刷新诊断地址并提供面板最后几行；根据首个 error/fatal 或缺失的后续阶段定位根因。

阻塞与证据：

- 等待用户返回当前浏览器中的运行时日志；`AmbientWorld.tsx` 和 `D:\project\blog` 均未修改。

## 2026-08-21 / v0.6 Creature Motion Smoothing

今天完成：

- 将原本每个显示帧一次的可变步长物理更新改为最多 5 个 120 Hz 子步，降低浏览器帧间隔波动对海豚脊柱的冲击。
- 海豚路线、斥力、脊柱、水母漂移、短触手和像素浮游物在同一模拟时钟内推进；暂停后模拟时钟冻结，恢复时不发生相位跳跃。
- 将物理推进与 Three.js 缓冲写入拆分，子步只更新内存状态，几何顶点、线段缓冲和实例矩阵每个显示帧仍只提交一次。
- 新增 30、60、120 FPS 等时长一致性测试，并增加 30 FPS 连续显示帧关节角变化上限测试。

当前现象：

- 30、60、120 FPS 模拟 8 秒后，路径相位和全部脊柱点在测试精度内一致。
- 低帧率和偶发掉帧时，头部位移被拆分后逐节传到尾部，不再把整帧折转一次性压入链式约束。
- 桌面实机 WebGPU 约 120 FPS 持续推进，暂停、继续和环游路径切换均正常，控制台无 warning 或 error。
- 移动端 390 x 844 为 `webgpu / ready`，横向溢出为 0，标题、控制条和状态栏无重叠。
- 本轮只修改独立 Demo、海豚纯函数模块、测试和文档；主页背景仍未集成。

下一步最小动作：

- 在独立 Demo 中复验完整 8 字和环游路径，由用户确认运动质感后再决定是否进入主页集成阶段。

阻塞与证据：

- Demo 仍受用户审核门禁约束；`AmbientWorld.tsx` 和 `D:\project\blog` 均未修改。

## 2026-08-21 / v0.6 Continuous Random Path Stream

今天完成：

- 根据用户指出的“路径一直转圈”问题，移除 8 字和椭圆闭合路线，以及路径相位取模回绕。
- 新增滚动开放路径流：消费一段后移除最旧引导点，并朝新的远距离随机目标补充等间距引导点，运行期间持续生成路线。
- 使用开放均匀三次 B 样条生成三次 Bézier 段，保持相邻段的位置、切线和曲率连续，并继续按弧长推进以稳定游速。
- 为边界处理加入前向曲率预测和转向惯性；`漫游`与`巡游`只改变随机性和转向节奏，两者都不再是固定圆周。
- 为脊柱链增加单位时间角速度上限，降低紧弯对尾部的瞬时冲击。
- 诊断面板新增 `route:extended` 与 `streamRevision`，重置和路径模式切换会记录不同的路线签名。

当前现象：

- 桌面 WebGPU 实测中 `streamRevision` 从 0 持续增长到 250 以上，随机目标多次变化；暂停后版本冻结，恢复后继续增长。
- 切换为`巡游`后按较缓参数生成新的开放路径；重置会生成不同路线签名，不复用初始路线。
- 移动端 390 x 844 为 `webgpu / ready`，页面横向溢出为 0；桌面和移动端均无 console warning/error。
- 纯函数测试覆盖 24 个种子各 400 段、500 段不回绕、700 段防小圆周、跨段 C2 连续、恒定弧长速度、航向连续与多帧率一致性。
- `bun test`：23 passed、0 failed；`bun run check`：0 errors、0 warnings、0 hints；`bun run build`：2 个静态路由构建成功。

下一步最小动作：

- 由用户继续在独立 Demo 中审核随机路线和身体传导；确认通过后才创建主页集成分支。

阻塞与证据：

- Demo 尚未获准集成主页；`AmbientWorld.tsx` 和 `D:\project\blog` 均未修改。

## 2026-08-22 / v0.6 Free-Roaming Path and Silhouette Guard

今天完成：

- 根据用户宽屏截图复现海豚轮廓失稳，确认旧测试只约束相邻关节，7 节局部弯角累计后最坏可达到约 99°。
- 没有把海豚锁成刚体；新增约 54° 的全身柔性保险线，同时保留尾段局部曲率和推进波。
- 将桌面和移动端路径活动区扩展到主视野之外，允许海豚短暂出屏后自然返回。
- 将边界力起点从活动区中心外 18% 移到外侧 34%，取消会中断模拟的硬墙，改用随越界距离增强的软回正与路径惩罚，消除长期指向中心造成的近似闭环。
- 将漫游和巡游的单点最大转向分别降至约 11.5° 与 7.2°，减小随机抖动并延长目标保持距离，使路线形成更舒展的长弧。
- 新增全身累计弯角与首尾弦长回归测试，覆盖长时间随机路线下的可辨识轮廓。

当前现象：

- 桌面连续观察中随机目标跨越左下、右侧和左上区域，海豚完成斜游、横游和竖游，未再围绕中心形成固定圆周。
- 移动端 390 x 844 为 `webgpu / ready`，无横向溢出；海豚可在视野边缘短暂进出并保持完整轮廓。
- 64 个种子各运行 12,000 个模拟步后，全身累计弯折受控，首尾弦长不低于身体长度约 94%，控制台无 warning/error。
- 12 个移动端种子各连续生成 700 段时均切换超过 20 个远目标、改变转向方向超过 35 次，避免退化为单向闭合圆周。
- `bun test`：26 passed、0 failed；`bun run check`：0 errors、0 warnings、0 hints；`bun run build`：2 个静态路由构建成功。

下一步最小动作：

- 由用户审核本轮自由游动节奏和姿态；通过后再考虑把独立 Demo 的生物行为集成到主页背景。

阻塞与证据：

- Demo 仍未获准集成主页；`AmbientWorld.tsx` 和 `D:\project\blog` 均未修改。

## 2026-08-22 / v0.6 Depth Layers and Jelly Pulse

今天完成：

- 将单个像素实例网格拆成远、中、近三个绘制层，并把深度连续映射到尺寸、透明度、漂移速度和交互响应。
- 海豚保持中景；约 13% 的近景像素在海豚之后绘制，形成偶发、克制的前景遮挡。
- 将水母的匀速漂移与正弦呼吸改为快收缩、慢舒张、收缩推进、阻尼滑行的周期行为，并让朝向平滑追随局部流场。
- 保留短触手链，推进时只增加小幅摆动；水母深度同步影响尺寸、透明度与绘制顺序。
- 为深度映射的单调性、脉冲连续性和推进窗口新增纯函数回归测试。

当前现象：

- 桌面深浅主题均能分辨远景小暗像素、中景普通像素和少量近景高对比像素；海豚不会被持续盖住。
- 水母不再只做同频正弦缩放，不同个体具有独立脉冲周期、朝向与推进节奏。
- 桌面 1280 x 720 与移动 390 x 844 均为 `webgpu / ready`，横向溢出为 0，干净浏览器会话无 console warning/error。
- `bun test`：28 passed、0 failed；`bun run check`：0 errors、0 warnings、0 hints；`bun run build`：2 个静态路由构建成功。

下一步最小动作：

- 由用户在独立 Demo 中审核纵深密度和水母生命感；确认后再决定是否进入主页集成分支。

阻塞与证据：

- 仍未修改 `AmbientWorld.tsx` 或 `D:\project\blog`，本轮只更新独立 Demo、共享纯函数、测试与文档。

## 2026-08-22 / v0.6 Homepage Creature Integration

今天完成：

- 用户确认独立 Demo 可以实装后，将完整 Demo 阶段以 `--no-ff` 合入 `develop`，再创建 `feature/v0.6-homepage-creature-integration`。
- 将海豚 Shape 提取为共享几何模块，保证实验页与正式页使用相同轮廓。
- 在主页 `AmbientWorld` 中用同侧程序化海豚替换旧鳐鱼，用脉冲水母替换旧鱼群，并将像素升级为远、中、近三层。
- 将海豚整条脊柱接入正文安全区；重叠时同时排斥和淡化，避免影响姓名、介绍和项目卡阅读。
- 保留原几何实体碰撞与碎裂，海豚可推动实体；卡牌切换事件继续触发背景脉冲和实体反馈。
- 静态降级替换为同源侧视海豚，不再显示旧鳐鱼轮廓；浅色主题小幅提高画布、流线、水母和实体边缘对比。

当前现象：

- 桌面 WebGPU 深浅主题、移动 WebGPU、桌面 WebGL2 和 static 均识别 `data-creature="same-side-dolphin"`。
- WebGPU 与 WebGL2 连续帧均有显著像素变化，正式页背景不是空白或冻结画布。
- 390 x 844 移动端横向溢出为 0；项目卡水合、切牌和背景联动在全新开发会话中正常。
- 所有最终验收会话均无 console warning/error。

下一步最小动作：

- 完成最终命令检查后提交集成分支，并以 `--no-ff` 合回 `develop`；不直接合入 `main`。

阻塞与证据：

- 姓名、人像、简历、邮箱和项目媒体仍是明确占位，需要真实素材后重新复验安全区；`D:\project\blog` 未修改。

## 2026-08-22 / v0.7 Marine Still Life and Fixed Simulation

今天完成：

- 将背景中的圆形、矩形和三角形渲染形态分别替换为贝壳、海玻璃和珊瑚碎枝，保留原碰撞、鼠标斥力、海豚推动与碎裂规则。
- 同步替换 static 降级视觉，避免低性能或减少动态模式退回抽象几何体。
- 将暗色主题海豚眼睛改为深墨色，并让眼睛随身体安全区透明度一起淡化。
- 新增 60 Hz 固定时间步调度器，最多追赶 8 个逻辑 tick，防止长帧恢复时出现计算螺旋。
- 将海豚、水母、粒子、静物、碰撞和碎裂更新放入固定 tick；显示帧仅负责累积时间、插值和渲染提交。
- 为海豚脊柱、静物位置与旋转、水母位置与朝向、像素位置保存前后状态并做帧间插值。

当前现象：

- 桌面 WebGPU 深浅主题均为 `full / ready`，海洋静物与深墨色眼睛诊断生效。
- 桌面 WebGL2 为 `balanced / ready`，static 显示贝壳、海玻璃、珊瑚和静态海豚。
- 移动端 390 x 844 为 `balanced / ready`，横向溢出为 0，固定 tick 持续推进。
- 30、60、120 FPS 等时长输入产生相同逻辑 tick 数；长帧最多执行 8 次追赶并丢弃过量时间。
- 浏览器控制台无 warning 或 error，项目切牌后背景继续运行并保留碎裂反馈。

下一步最小动作：

- 完成最终命令检查，提交功能分支并以 `--no-ff` 合回 `develop`；不合入 `main`。

阻塞与证据：

- 姓名、人像、简历、邮箱和项目媒体仍是明确占位；`D:\project\blog` 未修改。

## 2026-08-22 / v0.7.1 Ambient Contact Tuning

今天完成：

- 根据用户反馈移除尺寸失真的贝壳，将圆形实体改为原创五瓣有机海花轮廓。
- 将初始实体最大直径缩小约四成，static 降级同步替换为线框海花。
- 将实体恢复系数、边界反弹、鼠标斥力和碎裂分离速度整体下调，并提高线性与角速度阻尼。
- 海豚推动从逐帧累加冲量改为向目标速度收敛，避免接触持续时间决定击飞距离。
- 新增实体速度封顶纯函数，以及最大半径、最大速度和接触模型运行时诊断。

当前现象：

- 桌面 1280 x 720 初始最大实体半径不超过 `0.52`，移动 390 x 844 实测为 `0.415`。
- 连续快速移动鼠标并点击后，实测最高实体速度为 `0.484`，约一秒后衰减到 `0.078`。
- WebGPU 深浅主题、移动端和 static 均显示新的海花语义，没有旧贝壳节点。
- 页面横向溢出为 0，背景仍保持低于正文和人像占位的视觉层级。

下一步最小动作：

- 完成 WebGL2、控制台和构建回归后提交修复分支，并以 `--no-ff` 合回 `develop`。

阻塞与证据：

- 参考图仅用于形态语言，不复制受版权保护的具体图案；`D:\project\blog` 未修改。

## 2026-08-22 / v0.7.2 Gentle Ecosystem Budget

今天完成：

- 将珊瑚改为带位置与角度回正弹簧的稳定静物，碰撞和海豚推动的角冲量缩减为普通实体的 `8%`。
- 将水母鼠标响应从普通实体参数中拆出，缩小斥力范围和强度，提高阻尼，并增加 `0.24` 的独立速度上限。
- 允许最末级碎片继续受击并彻底消失，移除时同步释放实体专属几何体和材质。
- 用半径、面积与复杂度计算可破坏物权重，以初始权重的 `46%` 和 `102%` 建立补充滞回区间。
- 权重低于下限时从视口边缘随机补充静物，达到或轻微越过上限时停止；新实体低速进入并淡入。
- 新增速度封顶、碎裂权重与生成滞回纯函数测试，以及完整运行时诊断属性。

当前现象：

- WebGPU 长时间观察中，珊瑚最大偏角保持在约 `0-0.011 rad`，不再持续旋转。
- 水母日常最大速度约 `0.02-0.09`，远低于 `0.24` 硬上限，指针靠近只形成短促扰动。
- 同一运行会话中最末级实体累计消失 12 次，总权重越过下限后自动补充，生成停止后回到 `idle`。
- 桌面深浅主题、390 x 844 移动端、WebGL2 和 static 均保持原信息架构与背景层级。

下一步最小动作：

- 完成干净浏览器会话、生产构建和 Git 检查后提交修复分支，并以 `--no-ff` 合回 `develop`。

阻塞与证据：

- 未修改姓名、人像、简历、邮箱、ICP、许可证或项目内容；`D:\project\blog` 未修改。

## 2026-08-22 / v0.8 Narrative Section Motion

今天完成：

- 从 `develop` 创建 `feature/v0.8-narrative-section-motion`，以定向动效增强为边界，不调整信息架构、背景模拟或内容事实。
- 将静态文章列表改为独立 React 交互岛，三篇文章按交替方向重复进退场，并用 Motion value 与弹簧实现低幅度指针倾斜。
- 为文章增加原生按钮摘要开合、`aria-expanded`、`aria-controls` 和未来真实文章链接分支；占位文章不会伪装成可访问链接。
- 扩展原 `IntersectionObserver`，让关于、生活和联系区的子元素按顺序重播，并保留 `homepage:section-presence` 事件。
- 关于区使用标题落位、叙事段落错峰、轴线生长和节点弹入；生活区使用照片扇入与轻微 hover；联系区使用标题、命令和链接的短序列。
- 修复观察器提前写入 React 文章树导致的 hydration mismatch，让文章区完全由 Motion 管理，Astro 区块继续由观察器管理。
- 为 390 x 844 文章轨道增加水平裁切缓冲，将摘要触摸目标提高到 44 px，并补齐 `prefers-reduced-motion` 下的 hover 位移禁用。

当前现象：

- 桌面 WebGPU 深浅主题中，文章离开视口后行透明度回到 0，返回文章区后重新进入；关于区实测完成 `visible / hidden / visible` 状态循环。
- 文章摘要按钮会同步更新展开状态和真实占位说明，指针 hover 会产生低幅度三维变换，静态文章不制造虚假跳转。
- 390 x 844 WebGPU 中标题无裁切、按钮高度 44 px、文档横向溢出为 0；生活照片轨道保持独立横向滚动。
- WebGPU、WebGL2 与 static 的干净浏览器会话均保留 3 篇文章，控制台无 warning 或 error。

下一步最小动作：

- 完成最终命令门禁，提交功能分支并以 `--no-ff` 合回 `develop`；不合入 `main`。

阻塞与证据：

- 当前浏览器验收接口不能模拟 `prefers-reduced-motion` 媒体特性，低动态路径通过 Motion hook、CSS 媒体覆盖和静态代码审计确认。
- 姓名、人像、简历、邮箱、文章配图与真实文章链接仍为明确占位；`D:\project\blog` 未修改。

## 2026-08-23 / v0.9 Natural Still-life Drift

今天完成：

- 从 `develop` 创建 `fix/v0.9-ambient-natural-drift`，针对截图中的左侧边界堆积做机制级修复。
- 删除静物分裂队列、碎片创建和销毁、加权滞回补充，以及相关运行时诊断和纯函数。
- 将静物改为固定数量：桌面 8 个，平衡质量与移动端 6 个；使用分散槽位初始化并缩小最大尺寸。
- 用平滑洋流、个体相位扰动、低速阻尼和 `0.30` 速度上限模拟自然漂流。
- 删除硬边界夹取与反弹，改为带离屏余量的连续回流，并在回流时重置插值前态。
- 海豚接触改为渐进位置修正和限速排开，不再触发破坏；珊瑚保持位置漂流和小幅角度回正。
- 新增离屏回流与重复接触限速测试，并补充独立机制说明 `docs/V0.9_NATURAL_DRIFT.md`。

当前现象：

- 1440 x 900 WebGPU 暗色连续运行约 12 秒后保持 8 个实体，最高速度约 `0.052`，没有边缘排队。
- 390 x 844 WebGPU 保持 6 个实体，横向溢出为 0；深浅主题静物与海豚均正常。
- WebGL2 为 `balanced / ready` 且保持 6 个实体；static 为 `static / ready` 并显示原静态降级视觉。
- 干净 WebGPU 浏览器会话无 console warning 或 error；旧分裂与补充属性不再出现在运行时。

下一步最小动作：

- 完成最终 Git 门禁，提交修复分支并以 `--no-ff` 合回 `develop`；不合入 `main`。

阻塞与证据：

- 姓名、人像、简历、邮箱、文章配图与真实文章链接仍为明确占位；`D:\project\blog` 未修改。

## 2026-08-23 / v0.10 Biological Motion and Selective Glow

今天完成：

- 从 `develop` 创建 `fix/v0.10-ambient-biological-glow`，只修正背景叠层、水母生命感和局部发光，不调整信息架构与正文内容。
- 定位到动态后端准备完成后，CSS 仍以 `0.07` 不透明度保留整层 static fallback；将其改为完全淡出，消除 WebGPU 海豚下方的固定鱼影。
- 将水母周期缩短为约 `2.15-3.0 s`，增强钟形收缩与舒张差异，并加入随推进强度变化的朝向响应、惯性滑行和低幅度横摆。
- 让触手链继续使用固定步进模拟，在显示阶段叠加沿链传播的延迟波动，使触手跟随身体而不是整体刚性平移。
- 新增可测试的水母发光采样函数，亮度在推进阶段达到峰值，随后衰减为弱余辉。
- 使用现有 Three.js 材质增加选择性 additive halo：仅近景像素、海花、海玻璃、水母与海豚发光，珊瑚保持不发光。
- 深色主题使用克制的蓝色生物发光，浅色主题仅保留极弱高光；辉光随正文安全区可见度同步衰减。
- 补充 `data-glow-model`、`data-glow-theme`、`data-jelly-motion` 和 `data-static-overlay` 运行时诊断。

当前现象：

- WebGPU 与 WebGL2 均为 `ready`，动态模式 static fallback 计算不透明度为 `0`，固定鱼影不再出现。
- static 模式仍保留 `0.58` 的兜底层不透明度，静态海豚、海花、海玻璃、珊瑚和像素可见。
- 桌面深浅主题与 390 x 844 移动端没有横向溢出；浅色使用 `highlight`，深色使用 `bioluminescent`。
- 水母在多个完整周期中持续推进，固定逻辑 tick 正常增长，控制台无 warning 或 error。

下一步最小动作：

- 提交当前修复分支并以 `--no-ff` 合回 `develop`；不合入 `main`。

阻塞与证据：

- 姓名、人像、简历、邮箱、文章配图与真实文章链接仍为明确占位；`D:\project\blog` 未修改。

## 2026-08-23 / v0.11 Ambient Reveal and Chroma

今天完成：

- 从 `develop` 创建 `feature/v0.11-ambient-reveal-and-chroma`，保留既有太空海洋构图，只增强生物反馈、颜色层次和页面首次进入体验。
- 为 WebGPU、WebGL2、static 与初始化失败路径统一增加 `homepage:ambient-ready` 就绪信号。
- 新增像素水母、漂流像素和潮汐进度线加载门；背景完成后先独立展示，再让正文整体从下方缓入。
- 加载阶段正文带 `inert` 且不可见；设置最短显示、背景停留与 `4.8 s` 故障保护，避免首帧慢或渲染失败时卡死。
- 将水母推进倍率提高到 `3.05`，继续使用 `0.24` 独立速度上限，使收缩后产生可见的短促反冲滑行。
- 将水母发光静息值降到约 `0.035`，推进峰值提高到接近 `1`；新增核心和外层 additive halo，并同步改变轮廓、钟形体与触手亮度。
- 为水母和海洋静物加入确定性的低幅度蓝青色差；珊瑚增加轻微冷蓝层次但仍不发光。
- 为 Hero、区块标题与文章行增加无边框阅读柔化层；背景进入正文态后恢复原低饱和后处理。

当前现象：

- WebGPU 首帧完成后可观察到 `loading -> ambient -> entering -> ready`，完整背景阶段正文不显示且后处理为 0。
- 390 x 844 深浅主题均无实际横向滚动，Hero 标题完整，正文对比清晰。
- 水母推进诊断速度实测约 `0.17` 后自然衰减，没有撞上 `0.24` 上限。
- WebGL2 首帧较慢时加载门继续覆盖，画布完成过渡后 static fallback 降为 0；static 路径正常完成同一揭示序列。

下一步最小动作：

- 完成最终命令门禁，提交功能分支并以 `--no-ff` 合回 `develop`；不合入 `main`。

阻塞与证据：

- 当前浏览器接口不能模拟 `prefers-reduced-motion`，该路径通过 static 选择逻辑、CSS 媒体覆盖和代码审计确认。
- 姓名、人像、简历、邮箱、文章配图与真实文章链接仍为明确占位；`D:\project\blog` 未修改。

## 2026-08-23 / v0.12 Jelly Tow and Personal Visual Stage

今天完成：

- 从 `develop` 创建 `feature/v0.12-jelly-tow-and-visual-stage`，只升级首次进入叙事与 Hero 右侧视觉区域。
- 将 `setTimeout` 叠加的整页上浮替换为 Web Animations API 时间线，逐一等待水母入镜、触手张力、海流托举和首屏分层进入。
- 背景进入 `ambient` 后保留两个绘制帧和独立展示时间；导航、Hero 叙事与视觉舞台分别缓入，完成后才写入 `ready`。
- 新建 `HeroVisualStage.tsx`，将大型静态占位改为人物、工作现场和个人作品三个可扩展模式。
- 为视觉舞台加入语义 tab、前后按钮、方向键、触摸滑动、切换过渡和低幅度指针视差；持续指针值由 Motion value 管理。
- 调整桌面取景比例，使 `1280 x 720` 首屏可以看见完整模式轨道和图注；移动端使用稳定横向取景且无溢出。
- 补齐低动态、低透明度、浅色与深色主题覆盖，并保留所有真实素材占位边界。

当前现象：

- WebGPU 硬重载实测状态为 `loading 0.98 s -> ambient 2.66 s -> entering -> ready 4.41 s`，没有同帧闪现。
- `1280 x 720` 深浅主题中舞台底部约为 `695 px`，切换轨道、箭头和图注均位于首屏；横向溢出为 0。
- `390 x 844` 中舞台宽约 `327 px`，模式轨道宽约 `223 px`，按钮和图注完整，横向溢出为 0。
- WebGPU、WebGL2 与 static 都完成同一进入状态机；最终 WebGPU 深色会话无 console warning 或 error。

下一步最小动作：

- 提交功能分支并以 `--no-ff` 合回 `develop`；不合入 `main`。

阻塞与证据：

- 当前浏览器接口不能模拟 `prefers-reduced-motion`，该路径通过短淡入分支、Motion hook、CSS 媒体覆盖和代码审计确认。
- 姓名、人像、简历、邮箱、文章配图与真实文章链接仍为明确占位；`D:\project\blog` 未修改。

## 2026-08-23 / v0.13 Contact Signal Harbor

今天完成：

- 从 `develop` 创建 `feature/v0.13-contact-signal-harbor`，只重构联系区和对应的进入动效样式。
- 删除整区高饱和蓝色背景与孤立博客按钮，恢复半透明页面底色和太空海洋连续性。
- 将标题和入口说明改为非对称两栏，并加入短顶线、弧形海流、同心信号浮标和低幅像素节点。
- 将博客、GitHub、简历和邮箱重排为一条带分隔线的联系轨道；博客作为主要阅读入口，后三项形成次级纵向序列。
- 使用 Phosphor 图标区分入口；真实链接提供局部背景、图标与箭头反馈，简历和邮箱保持明确 `PENDING` 状态。
- 移动端改为四条单列轨道，页脚使用两列加备案号独占一行的结构。

当前现象：

- `1280 x 720` 深浅主题中联系区约高 `791 px`，结构线、标题和背景生物保持清晰层级。
- `390 x 844` 浅色主题中轨道约宽 `335 px`、区块约高 `933 px`，四个入口、域名与备案号完整显示。
- 桌面与移动端文档横向溢出为 0；WebGPU 深色最终会话无 console warning 或 error。
- 博客入口 hover 计算背景为 9% 蓝色，图标抬升 `3 px` 并旋转 `-2 deg`，箭头移动 `(3 px, -3 px)`。

下一步最小动作：

- 提交功能分支并以 `--no-ff` 合回 `develop`；不合入 `main`。

阻塞与证据：

- 姓名、简历、邮箱、生活照片与部分作品证据仍为明确占位；`D:\project\blog` 未修改。

## 2026-08-23 / v0.14 Jelly Tow Timing and Soft Motion

今天完成：

- 从 `develop` 创建 `feature/v0.14-jelly-tow-and-motion`，只调整首次进入编舞与背景水母运动。
- 将正常开场编排为水母入镜、触手张力、同步牵引和缓慢脱离；最长牵引动画由约 `1.68 s` 延长到约 `3.01 s`。
- 用多段关键帧限制开场前段位移，正文不再在第一个强 ease-out 段内完成大部分上升。
- 保留快速故障保护路径与 reduced motion 短淡入，不让降级路径承担完整叙事时长。
- 新增 `sampleJellyPose`，统一钟形体缩放、触手根部位置和末端波幅，并补充幅度与周期连续性测试。
- 降低水母推进倍率、转向响应和显示层独立横摆；触手摆幅改为跟随速度与推进相位。
- 保留 `60 Hz` 固定逻辑步进与显示插值，未把连续值写入 React state。
- 增加开场阶段和总时长诊断属性，便于后续回归。

当前现象：

- WebGPU 移动端为 `balanced / ready`，两只水母保持固定步进，观察到的最大速度约 `0.059`。
- WebGL2 为 `balanced / ready`，水母速度保持在 `0.24` 上限以内；static 为 `static / ready` 且兜底层不透明度为 `0.58`。
- `1280 x 720` 深浅主题与 `390 x 844` 浅色主题没有横向溢出，背景水母在浅色下仍可辨认。

下一步最小动作：

- 完成最终构建、Git 门禁和功能分支提交，以 `--no-ff` 合回 `develop`；不合入 `main`。

阻塞与证据：

- 姓名、人像、简历、邮箱、文章配图与真实文章链接仍为明确占位；`D:\project\blog` 未修改。

## 2026-08-23 / v0.15 Physical Page Tow

今天完成：

- 从 `develop` 创建 `fix/v0.15-physical-page-tow`，修复开场水母与页面没有真实牵引关系的问题。
- 删除导航、Hero 文案和视觉舞台的三套独立进入动画，改为完整 `[data-site-content]` 统一从 `96vh` 上浮到最终位置。
- 将页面前沿、水母和正文绑定到同一组位移，水母触手长度按视口高度约束，主牵引期间前沿与水母保持固定距离。
- 将速度拆分为 `580 ms` 张力承载和 `2100 ms` 主牵引，分别使用对称平滑与慢起慢停贝塞尔曲线。
- 为深链接开场加入基于当前 `scrollY` 的静态裁切边界，`#contact` 刷新时当前区块从前沿下方随整页进入。
- 将普通区块进退场延后到体验门完成后初始化，避免开场期间正文被观察器隐藏，并保留后续重复滚动显现。
- `loading`、`ambient`、`entering` 全程锁定滚动，正文到 `ready` 才移除 `inert` 并清理临时样式。

当前现象：

- `1280 x 720` WebGPU 中段采样显示正文与页面前沿的 `Y` 坐标逐帧一致，水母稳定领先约 `203 px`。
- `#contact` 深链接在约 `scrollY=4277` 时使用对应裁切边界，当前联系区文字随前沿进入，完成后保持原滚动位置。
- `390 x 844` 中段正文与前沿均位于约 `573 px`，水母位于约 `335 px`，触手连接关系清楚且无有效横向滚动。
- WebGPU、WebGL2 和 static 均完成 `ambient -> entering -> ready`，正常编舞实测约 `5.2 s`。

下一步最小动作：

- 完成最终构建与 Git 门禁，提交修复分支并以 `--no-ff` 合回 `develop`；不合入 `main`。

阻塞与证据：

- 当前浏览器接口不能模拟 `prefers-reduced-motion`，该路径通过短淡入分支和 CSS 媒体覆盖代码审计确认。
- 姓名、人像、简历、邮箱、文章配图与真实文章链接仍为明确占位；`D:\project\blog` 未修改。

## 2026-08-23 / v0.16 Reveal Preroll Guard

今天完成：

- 从 `develop` 创建 `fix/v0.16-hide-reveal-preroll`，修复背景独立展示结束后，牵引水母会在入场动画开始前短暂出现在右上角的问题。
- 将水母的 CSS 初始姿态固定为 `opacity: 0` 和 `translate3d(0, 112vh, 0)`，与 `arrival` 的首个关键帧一致。
- 将海流前沿的 CSS 初始姿态固定为 `opacity: 0` 和 `translate3d(0, 100vh, 0)`，避免同一时序窗口暴露顶部默认位置。

当前现象：

- `data-choreography="active"` 且 `arrival` 尚未开始时，水母和海流前沿都保持透明并位于视口下方。
- `#about` 深链接与普通首屏刷新共享相同的安全初始姿态，慢帧不再暴露右上角水母。

下一步最小动作：

- 完成自动化门禁，提交修复分支并以 `--no-ff` 合回 `develop`；不合入 `main`。

阻塞与证据：

- 姓名、人像、简历、邮箱、文章配图与真实文章链接仍为明确占位；`D:\project\blog` 未修改。

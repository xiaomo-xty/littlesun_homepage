# 开发日志

## 2026-08-16 / Repository

今天完成：

- 确认 Penpot V6 为实现基线。
- 完整阅读职业发展文件，提取内容边界。
- 只读审计现有博客的蓝色主题、面板层次与主题切换方式。
- 初始化独立 Git 仓库并定义分支流程。

当前现象：

- `D:\project\homepage` 是新仓库，尚未建立 Astro 工程。
- 姓名、人像、简历、GitHub 和邮箱均未提供。
- 文章题目来自职业规划中的候选方向，尚未确认真实发布状态。

下一步最小动作：

- 从 `develop` 创建 `feature/foundation`，建立可运行的 Astro + Bun 骨架和主题变量。

阻塞与证据：

- 无阻塞。

新增 Backlog：

- 用真实资料替换个人占位符。
- 用真实 SceneScope 截图替换项目截图槽位。
- 接入已发布文章的真实链接和发布日期。

## 2026-08-16 / Foundation

今天完成：

- 建立 Astro 5 + Bun 工程与 React island 支持。
- 建立浅色、深色语义变量和系统主题首屏初始化。
- 自托管中文、拉丁与局部像素字体。
- 建立项目、文章和站点信息的数据模型。
- 完成语义页面骨架、SEO 基线和明确个人资料占位符。

当前现象：

- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：静态首页构建成功。
- 依赖安装首次遇到注册表连接拒绝，切换镜像后完成并生成 `bun.lock`。

下一步最小动作：

- 将静态骨架重构为 V6 叙事布局，完成响应式导航、关于我、文章、生活和联系区域。

阻塞与证据：

- 无阻塞。

新增 Backlog：

- 增加可访问的主题切换控件。
- 将作品列表改造成可扩展卡组，不把数量写死在视图层。

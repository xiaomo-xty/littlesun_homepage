# 开发日志

## 2026-08-16 / Repository

今天完成：

- 确认 Penpot V6 为实现基线。
- 完整阅读职业发展文件，提取内容边界。
- 只读审计现有博客的蓝色主题、面板层次与主题切换方式。
- 初始化独立 Git 仓库并定义分支流程。

当前现象：

- `D:\project\homepage` 是新仓库，尚未建立 Astro 工程。
- 姓名、人像、简历、GitHub 和邮箱均未提供。
- 文章题目来自职业规划中的候选方向，尚未确认真实发布状态。

下一步最小动作：

- 从 `develop` 创建 `feature/foundation`，建立可运行的 Astro + Bun 骨架和主题变量。

阻塞与证据：

- 无阻塞。

新增 Backlog：

- 用真实资料替换个人占位符。
- 用真实 SceneScope 截图替换项目截图槽位。
- 接入已发布文章的真实链接和发布日期。

## 2026-08-16 / Foundation

今天完成：

- 建立 Astro 5 + Bun 工程与 React island 支持。
- 建立浅色、深色语义变量和系统主题首屏初始化。
- 自托管中文、拉丁与局部像素字体。
- 建立项目、文章和站点信息的数据模型。
- 完成语义页面骨架、SEO 基线和明确个人资料占位符。

当前现象：

- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：静态首页构建成功。
- 依赖安装首次遇到注册表连接拒绝，切换镜像后完成并生成 `bun.lock`。

下一步最小动作：

- 将静态骨架重构为 V6 叙事布局，完成响应式导航、关于我、文章、生活和联系区域。

阻塞与证据：

- 无阻塞。

新增 Backlog：

- 增加可访问的主题切换控件。
- 将作品列表改造成可扩展卡组，不把数量写死在视图层。

## 2026-08-16 / Narrative

今天完成：

- 按 Penpot V6 重构首屏、关于我、作品、文章、生活与联系区域。
- 建立人物素材、项目截图、文章缩略图和生活照片的明确占位区域。
- 增加浅色与深色主题切换、移动端全屏导航和 Escape 关闭行为。
- 使用像素字体、像素角标、空间轴与叠层表达技术气质，不使用受版权保护的角色素材。
- 作品数据仍由数组驱动，页面没有写死三张卡牌的上限。

当前现象：

- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：静态首页与 React 导航 island 构建成功。
- 当前作品卡是静态首张状态，按钮行为将在交互阶段接入。

下一步最小动作：

- 在独立交互分支实现卡牌切换、翻面、触摸滑动和键盘操作。

阻塞与证据：

- 无阻塞。

新增 Backlog：

- 为首屏加入自适应 Three.js 世界背景。
- 为类型状态加入克制的打字机效果。
- 为卡牌切换加入像素粒子反馈和 reduced-motion 降级。

## 2026-08-16 / Interaction

今天完成：

- 用 Three.js 建立铺满首屏的实时世界背景，并按设备能力调整像素粒子数量和 DPR。
- 在页面不可见、标签页隐藏或系统要求减少动态效果时暂停或降级动画。
- 为职业方向加入打字机状态行，保留静态文本作为无脚本和 reduced-motion 回退。
- 将代表项目改造成数据驱动的可扩展卡组，项目数量不受视图层限制。
- 完成分类筛选、循环切牌、卡牌翻面、指针倾斜、触摸滑动和键盘操作。
- 卡牌背面统一呈现问题、过程与证据，切换时加入克制的像素粒子反馈。

当前现象：

- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：静态构建成功，Three.js、Motion 与 React islands 正常产出。
- WebGL 不可用时保留静态背景；交互控件具备可见焦点和语义标签。

下一步最小动作：

- 从 `develop` 创建独立测试分支，记录桌面、移动端、主题、交互和画布验收结果。

阻塞与证据：

- 无阻塞。

新增 Backlog：

- 用真实项目截图和可公开的商业开发证据替换占位内容。
- 根据正式内容长度复核卡牌背面排版与筛选分类。
- 后续评估是否用 wgpu/Wasm 替换首屏渲染实验，但不纳入首版范围。

## 2026-08-16 / Browser QA

今天完成：

- 在 1440 x 900 桌面视口和 390 x 844 移动视口完成深浅主题视觉验收。
- 验证移动菜单、项目筛选、循环切牌、翻面、触摸滑动与键盘操作。
- 检查文字裁切、元素重叠、水平溢出、颜色对比度和首屏章节提示。
- 验证 Three.js 画布非空、可见性暂停、reduced-motion 和静态背景降级。
- 将验收矩阵、数值证据和内容边界写入 `docs/QA_REPORT.md`。

当前现象：

- 浏览器控制台无 warning 或 error。
- 桌面与移动端均无水平溢出；关键交互均通过鼠标、触摸或键盘验证。
- 姓名、联系信息、项目图片与文章仍是明确占位内容，不影响原型验收。

下一步最小动作：

- 在 `develop` 重新运行完整检查，合入 `main` 并标记 `v0.1.0`。

阻塞与证据：

- 无阻塞。

新增 Backlog：

- 获得真实个人资料后建立独立内容分支替换占位符。
- 接入正式域名之前补充站点图标、Open Graph 图片和部署配置。

## 2026-08-16 / Card Deck Motion

今天完成：

- 修正正反面在指针倾斜状态下产生不同透视位置的问题，翻面前统一归零外层倾斜。
- 将正反面改为同一固定卡高和同一套五行网格，标题、内容区、技术栈与翻面按钮共享空间基准。
- 移除串行等待切牌，改为旧牌退场与新牌从后方上桌同时发生的 spring 旋转过渡。
- 用两张真实项目预览替换装饰性卡片阴影，只渲染当前牌和后两张，数据规模仍不受限制。
- 后牌支持点击与键盘切换，并保留方向键、翻面键和移动端拖动操作。
- 修复旋转后的后牌覆盖下方切牌按钮、导致一次点击跳过两张的问题。
- 翻面后将键盘焦点移动到当前可见面的控制按钮。

当前现象：

- 桌面端正反面边界均为 `520 x 650`，坐标完全一致。
- 移动端正反面边界均为 `321 x 600`，四张项目卡的正面和证据区均无内容溢出。
- 1440 x 900 与 390 x 844 视口均无页面水平溢出。
- `bun run check`：0 errors、0 warnings、0 hints。
- `bun run build`：生产构建成功。

下一步最小动作：

- 在独立测试分支记录卡组补丁验收结果，合入 `develop` 后发布 `v0.1.1`。

阻塞与证据：

- 无阻塞。

新增 Backlog：

- 接入真实项目图片后复核不同图片明暗对卡牌空间层级的影响。
